# 🎯 Решение проблемы "Failed to fetch"

## Проблема
Demo UI показывает ошибку "Error: Failed to fetch" при попытке подключиться к API.

## Причина
CORS (Cross-Origin Resource Sharing) блокирует запросы из HTML файлов (file://) к localhost API.

## ✅ Решение (Уже применено!)

### 1. CORS исправлен в backend/main.py
```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"],  # Разрешены все origins
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### 2. Сервер автоматически перезагрузился (hot-reload)

## 🔧 Как использовать

### Способ 1: Обновить Demo UI с очисткой кэша
1. Откройте `frontend/demo.html` в браузере
2. Нажмите **Ctrl + Shift + R** (или Cmd + Shift + R на Mac)
3. Это очистит кэш и загрузит новые CORS настройки
4. Попробуйте снова нажать "Run Backtest"

### Способ 2: Использовать тестовую страницу
1. Откройте `frontend/test.html` (уже открыта!)
2. Нажмите кнопки для тестирования разных endpoints
3. Если работает - значит API полностью функционален

### Способ 3: Swagger UI (самый надёжный)
1. Откройте http://localhost:8000/docs
2. Все endpoints доступны для тестирования
3. Не требует CORS, работает всегда

## 📊 Проверка что всё работает

### PowerShell тест:
```powershell
# Проверить health
Invoke-RestMethod "http://localhost:8000/health"

# Получить символы
Invoke-RestMethod "http://localhost:8000/api/v1/data/symbols"

# Быстрый бэктест
Invoke-RestMethod "http://localhost:8000/api/v1/backtest/quick/BTCUSDT/D?days=60"
```

### Браузер тест:
1. Откройте http://localhost:8000/api/v1/data/symbols
2. Должен вернуть JSON с символами

## 🐛 Если проблема осталась

### Вариант A: Очистить кэш браузера полностью
1. **Chrome/Edge**: Ctrl+Shift+Delete → Очистить кэш и cookies
2. **Firefox**: Ctrl+Shift+Delete → Кэш
3. Перезапустить браузер
4. Открыть demo.html снова

### Вариант B: Использовать другой браузер
Попробуйте открыть demo.html в другом браузере (Chrome, Firefox, Edge)

### Вариант C: Проверить консоль браузера
1. Нажмите F12 (Developer Tools)
2. Перейдите на вкладку "Console"
3. Попробуйте кнопку в Demo UI
4. Посмотрите на ошибки - там будет точная причина

### Вариант D: Запустить simple HTTP server
```powershell
# Перейти в папку frontend
cd D:\bybit_strategy_tester_v2\frontend

# Запустить Python HTTP server
python -m http.server 8080

# Открыть в браузере
Start-Process "http://localhost:8080/demo.html"
```

Теперь файл будет загружаться через http://, а не file://, что гарантированно решит CORS проблему.

## 🎉 Итог

**API полностью работает!** Проблема была только в CORS при загрузке через file://.

### Рабочие способы доступа:
1. ✅ **Swagger UI** - http://localhost:8000/docs (100% работает)
2. ✅ **PowerShell** - команды выше (100% работает)
3. ✅ **test.html** - простая тестовая страница (100% работает после обновления)
4. ⚠️ **demo.html** - может требовать очистку кэша браузера

### Рекомендация:
**Используйте Swagger UI** (http://localhost:8000/docs) - это самый надёжный способ тестирования API без проблем с CORS.

## 📝 Следующие шаги

После того как убедитесь что API работает:

1. **Попробуйте разные стратегии** через Swagger UI
2. **Измените параметры** (capital, leverage, days)
3. **Посмотрите разные символы** (ETHUSDT, SOLUSDT)
4. **Экспериментируйте с интервалами** (15m, 1h, D)

Удачи! 🚀
