# ✅ Тест Zustand Persistence - Quick Win #5

## 🎯 Цель теста
Проверить, что все 4 store сохраняют данные в localStorage и восстанавливают их после перезагрузки.

## 📋 Тестовый сценарий

### 1. Открыть приложение
- URL: http://localhost:5173/
- Откройте DevTools (F12) → вкладка Console

### 2. Проверка backtests store
```javascript
// Добавить фильтр
localStorage.getItem('backtests-store')
// Должно показать: {"state": {"items": [...], "filter": "..."}, "version": 0}
```

### 3. Проверка strategies store
```javascript
localStorage.getItem('strategies-store')
// Должно показать: {"state": {"items": [...], "filter": "..."}, "version": 0}
```

### 4. Проверка optimizations store
```javascript
localStorage.getItem('optimizations-store')
// Должно показать: {"state": {"items": [...], "filter": "..."}, "version": 0}
```

### 5. Проверка bots store (НОВЫЙ)
```javascript
localStorage.getItem('bots-store')
// Должно показать: {"state": {"items": [...], "filter": "..."}, "version": 0}
```

### 6. Тест persistence
1. В UI измените любые данные (добавьте фильтр, создайте item)
2. Перезагрузите страницу (F5)
3. Проверьте, что изменения сохранились

### 7. Проверка всех ключей localStorage
```javascript
// Показать все ключи Zustand
Object.keys(localStorage).filter(k => k.endsWith('-store'))
// Должно быть: ["backtests-store", "strategies-store", "optimizations-store", "bots-store"]
```

## ✅ Критерии успеха
- [ ] Все 4 store имеют ключи в localStorage
- [ ] Данные сохраняются после изменений
- [ ] Данные восстанавливаются после F5
- [ ] Версионирование работает (version: 0)

## 🎉 Результат
После выполнения всех шагов - Quick Win #5 считается полностью завершённым и протестированным!
