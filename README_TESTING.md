# 🧪 Тестовая инфраструктура для системы кэширования

## 📁 Файлы тестирования

| Файл | Назначение |
|------|------------|
| **test_storage.js** | Тестовый скрипт для загрузки в консоль браузера |
| **TESTING_PLAN.md** | Подробный план тестирования с инструкциями |
| **QUICK_TEST_GUIDE.md** | Быстрая шпаргалка для тестирования |
| **EXPECTED_RESULTS.md** | Примеры ожидаемых результатов всех тестов |
| **README_TESTING.md** | Этот файл (обзор тестовой инфраструктуры) |

---

## 🚀 Быстрый старт (30 секунд)

### 1. Запустить приложение
```powershell
cd d:\bybit_strategy_tester_v2\frontend
npm run dev
```
✅ Приложение будет на http://localhost:5173

### 2. Открыть консоль браузера
1. Откройте http://localhost:5173
2. Нажмите **F12** → вкладка **Console**

### 3. Загрузить тестовые утилиты
```javascript
// Скопируйте ВЕСЬ код из frontend/test_storage.js
// Вставьте в консоль и нажмите Enter
```

### 4. Запустить первый тест
```javascript
AutoTests.test1_EmptyStorage()
// Затем: Ctrl+R (перезагрузка страницы)
```

---

## 📚 Документация

### Для начинающих
👉 Читайте **QUICK_TEST_GUIDE.md** - быстрая шпаргалка со всеми командами

### Для детального тестирования
👉 Читайте **TESTING_PLAN.md** - полный план с 7 основными и 7 дополнительными тестами

### Для сравнения результатов
👉 Читайте **EXPECTED_RESULTS.md** - примеры ожидаемых логов и данных

---

## 🎯 Что тестируется

### Основная функциональность
1. ✅ Загрузка свечей в пустую базу (до 1000 из-за лимита API)
2. ✅ Переключение таймфреймов с отдельным кэшем
3. ✅ Догрузка новых свечей к существующему кэшу
4. ✅ Обновление последних 10 свечей (предотвращение гэпов)
5. ✅ Ограничение кэша до 2000 свечей
6. ✅ Корректное сохранение при выходе (кнопка "Выход")

### Дополнительные сценарии
7. ✅ Работа с несколькими таймфреймами одновременно
8. ✅ Переключение между торговыми парами
9. ✅ Обработка устаревшего кэша (>7 дней)
10. ✅ Обработка сетевых ошибок
11. ✅ Быстрое переключение (race conditions)
12. ✅ Обработка поврежденного кэша

---

## 🛠️ Тестовые утилиты

### TestUtils - Управление кэшем

```javascript
// Очистка
TestUtils.clearAllStorage()                    // Удалить ВСЕ данные
TestUtils.clearSymbol('BTCUSDT', '15')         // Удалить конкретную пару

// Просмотр
TestUtils.showStats()                          // Показать статистику

// Создание тестовых данных
TestUtils.createFakeCache('BTCUSDT', '15', 500)      // Создать 500 свечей
TestUtils.partialClear('BTCUSDT', '15', 200)   // Удалить последние 200
```

### AutoTests - Автоматические тесты

```javascript
AutoTests.test1_EmptyStorage()           // Тест 1: Пустая база
AutoTests.test2_EmptyStorage1h()         // Тест 2: Пустая база + 1h
AutoTests.test3_PartialStorage()         // Тест 3: Догрузка
AutoTests.test4_PartialStorage1h()       // Тест 4: Догрузка + 1h
AutoTests.test5_Historical()             // Тест 5: Исторические данные
AutoTests.test6_GracefulShutdown()       // Тест 6: Кнопка "Выход"
AutoTests.test7_MultipleTimeframes()     // Тест 7: Несколько таймфреймов
AutoTests.test8_SymbolSwitch()           // Тест 8: Смена пар
AutoTests.test9_CacheLimits()            // Тест 9: Лимит 2000
```

---

## 📊 Интерпретация логов

### Эмодзи в консоли

| Эмодзи | Значение | Критичность |
|--------|----------|-------------|
| 📦 | Найден кэш в localStorage | ℹ️ Инфо |
| 🆕 | Кэш не найден, загрузка новых данных | ℹ️ Инфо |
| 📊 | Загрузка с API Bybit | ℹ️ Инфо |
| 🕐 | Временные метки | ℹ️ Инфо |
| ✅ | Операция успешна | ✅ Успех |
| 💾 | Сохранение в localStorage | ℹ️ Инфо |
| ⚠️ | Предупреждение (не критично) | ⚠️ Внимание |
| ❌ | Ошибка | ❌ Критично |
| 🗑️ | Удаление данных | ℹ️ Инфо |

### Примеры логов

#### ✅ Хороший лог (первая загрузка)
```
🆕 No cache found, loading fresh data: bybit_candles_v1_linear_BTCUSDT_15
📊 Fetching 1000 candles from API...
✅ Loaded 1000 candles
💾 Saved 1000 candles to localStorage
```

#### ✅ Хороший лог (догрузка)
```
📦 Found 600 cached candles for: bybit_candles_v1_linear_BTCUSDT_15
🕐 Oldest: 2025-01-15T10:00:00Z, Newest: 2025-01-20T14:00:00Z
✅ Updated last 10 candles
📊 New candles needed: 150 (time diff: 135000 seconds)
📊 Fetching 150 new candles from API...
💾 Saved 750 candles to localStorage
```

#### ⚠️ Предупреждение (не критично)
```
⚠️ Requested 2000 historical candles but only 1000 in cache
⚠️ Note: Bybit API doesn't support fetching older data
💡 Consider implementing backend service
```

#### ❌ Ошибка (требует внимания)
```
❌ Failed to load candles: Network request failed
❌ ERROR: Failed to parse localStorage data
```

---

## 🔍 Отладка проблем

### Проблема: Данные не загружаются

**Диагностика:**
```javascript
// 1. Проверить наличие кэша
TestUtils.showStats()

// 2. Проверить конкретный ключ
localStorage.getItem('bybit_candles_v1_linear_BTCUSDT_15')

// 3. Проверить сеть (DevTools → Network)
```

**Решение:**
```javascript
// Очистить и попробовать снова
TestUtils.clearAllStorage()
// Ctrl+R (перезагрузка)
```

---

### Проблема: График не отображается

**Диагностика:**
```javascript
// Проверить консоль на ошибки
// Проверить React DevTools

// Проверить данные в сторе
const store = window.__ZUSTAND_STORE__?.getState();
console.log(store.candles);
```

**Решение:**
```javascript
// Очистить кэш конкретной пары
TestUtils.clearSymbol('BTCUSDT', '15')
// Ctrl+R
```

---

### Проблема: Слишком много данных в localStorage

**Диагностика:**
```javascript
TestUtils.showStats()
// Посмотреть на "💾 Общий размер"
// Если > 5 MB - проблема
```

**Решение:**
```javascript
// Удалить старые/ненужные данные
TestUtils.clearSymbol('ETHUSDT', '15')  // Если не используется
TestUtils.clearSymbol('SOLUSDT', '60')  // Если не используется

// Или очистить всё
TestUtils.clearAllStorage()
```

---

### Проблема: Дублирование свечей

**Диагностика:**
```javascript
// Посмотреть данные
const key = 'bybit_candles_v1_linear_BTCUSDT_15';
const data = JSON.parse(localStorage.getItem(key));
console.log(data.candles.length);

// Проверить на дубликаты
const times = data.candles.map(c => c.time);
const uniqueTimes = new Set(times);
console.log('Всего:', times.length, 'Уникальных:', uniqueTimes.size);
```

**Решение:**
```javascript
// Очистить и перезагрузить
TestUtils.clearSymbol('BTCUSDT', '15')
// Ctrl+R
```

---

## 📈 Ожидаемая производительность

### Нормальные значения

| Операция | Время | Примечание |
|----------|-------|------------|
| Загрузка 1000 свечей с API | 1-3 сек | Зависит от сети |
| Загрузка из localStorage | <100 мс | Почти мгновенно |
| Переключение таймфрейма (с кэшем) | <50 мс | Мгновенно |
| Сохранение в localStorage | <200 мс | Быстро |
| Дедупликация 2000 свечей | <100 мс | Быстро |

### Медленные значения (требуют внимания)

| Операция | Время | Проблема |
|----------|-------|----------|
| Загрузка с API | >10 сек | Проблема с сетью/сервером |
| Загрузка из localStorage | >1 сек | Слишком большой кэш |
| Переключение таймфрейма | >1 сек | Проблема с React |
| Сохранение | >2 сек | Слишком большие данные |

---

## 📝 Отчет о тестировании

### Шаблон отчета

```markdown
# Отчет о тестировании - [Дата]

## Окружение
- ОС: Windows 11
- Браузер: Chrome 120.0
- Node.js: v20.11.0
- Приложение: v2.0.0

## Результаты

| Тест | Статус | Время | Примечания |
|------|--------|-------|------------|
| 1    | ✅ PASS | 2.5 сек | OK |
| 2    | ✅ PASS | 1.8 сек | OK |
| 3    | ⚠️ PARTIAL | 8.2 сек | Медленная догрузка |
| ...  | ...    | ...   | ... |

## Найденные баги
1. [BUG-001] Медленная загрузка при большом кэше
2. [BUG-002] ...

## Рекомендации
- Оптимизировать дедупликацию
- Добавить индикатор загрузки
- ...
```

---

## 🎓 Обучение

### Для новых участников команды

1. **День 1:** Прочитайте QUICK_TEST_GUIDE.md
2. **День 2:** Запустите все AutoTests
3. **День 3:** Изучите EXPECTED_RESULTS.md
4. **День 4:** Прочитайте полный TESTING_PLAN.md
5. **День 5:** Проведите полное тестирование

### Полезные ссылки

- **localStorage API:** https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage
- **Bybit API docs:** https://bybit-exchange.github.io/docs/
- **Zustand:** https://github.com/pmndrs/zustand
- **React DevTools:** https://react.dev/learn/react-developer-tools

---

## 🔄 Continuous Integration

### Автоматизация (будущее)

```yaml
# Пример CI/CD pipeline (для будущей интеграции)
test:
  script:
    - npm run test
    - npm run test:integration
    - npm run test:e2e
```

### Playwright/Cypress интеграция

```javascript
// Будущий E2E тест
test('should load candles from localStorage', async ({ page }) => {
  await page.goto('http://localhost:5173');
  await page.evaluate(() => {
    TestUtils.createFakeCache('BTCUSDT', '15', 1000);
  });
  await page.reload();
  const stats = await page.evaluate(() => TestUtils.showStats());
  expect(stats).toContain('1000');
});
```

---

## 📞 Контакты

**Вопросы по тестированию:**
- Создайте issue в репозитории
- Отправьте PR с улучшениями тестов

**Нашли баг?**
1. Воспроизведите с помощью AutoTests
2. Запишите логи консоли
3. Создайте баг-репорт

---

## 📋 Чек-лист релиза

Перед каждым релизом:

- [ ] Запущены все AutoTests (test1-test9)
- [ ] Проверены edge cases (test7.4-test7.7)
- [ ] Проверена производительность
- [ ] localStorage не переполнен
- [ ] Нет утечек памяти
- [ ] Все логи корректны
- [ ] Документация обновлена
- [ ] Баги исправлены

---

## 🎯 Критерии качества

### ✅ Готово к релизу:
- Все тесты PASS
- Производительность в норме
- Нет критичных багов
- Документация актуальна

### ⚠️ Нужны доработки:
- Некоторые тесты PARTIAL
- Есть некритичные баги
- Производительность приемлемая

### ❌ Не готово:
- Тесты FAIL
- Критичные баги
- Плохая производительность
- Утечки памяти

---

**Удачного тестирования! 🚀**
