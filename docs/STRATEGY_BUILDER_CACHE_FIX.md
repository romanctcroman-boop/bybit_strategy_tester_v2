# 🔧 Strategy Builder - Исправление проблемы с кэшем браузера

> **Дата**: 2026-01-29  
> **Проблема**: Синтаксическая ошибка все еще появляется после исправления

---

## 🔍 Возможная причина

Браузер может использовать **кэшированную версию** JavaScript файла, даже после того, как файл был исправлен на сервере.

---

## ✅ Решение

### Шаг 1: Очистите кэш браузера

**Chrome/Edge:**
1. Откройте DevTools (F12)
2. Правой кнопкой мыши на кнопку обновления страницы
3. Выберите "Очистить кэш и жесткая перезагрузка" (Empty Cache and Hard Reload)

**Или используйте:**
- `Ctrl+Shift+Delete` → Очистить кэш
- `Ctrl+F5` → Жесткая перезагрузка
- `Ctrl+Shift+R` → Перезагрузка с очисткой кэша

### Шаг 2: Проверьте, что файл обновлен

В консоли браузера выполните:

```javascript
// Проверить, когда файл был загружен
const scripts = Array.from(document.scripts);
const strategyBuilderScript = scripts.find(s => s.src.includes('strategy_builder.js'));
console.log('Script URL:', strategyBuilderScript?.src);
console.log('Script loaded:', strategyBuilderScript ? 'Yes' : 'No');

// Попробовать загрузить файл напрямую
fetch('/frontend/js/pages/strategy_builder.js')
  .then(r => r.text())
  .then(text => {
    const lines = text.split('\n');
    console.log('Line 830:', lines[829]); // Индекс 829 = строка 830
    console.log('Line 831:', lines[830]);
    console.log('Line 832:', lines[831]);
  });
```

### Шаг 3: Принудительная перезагрузка скрипта

Если проблема сохраняется, добавьте версию к URL скрипта в HTML:

```html
<script src="./js/pages/strategy_builder.js?v=20260129_2"></script>
```

---

## 🧪 Проверка синтаксиса

### В консоли браузера:

```javascript
// Проверить синтаксис загруженного скрипта
try {
  const script = document.querySelector('script[src*="strategy_builder.js"]');
  if (script) {
    console.log('Script found:', script.src);
    // Попробовать выполнить код
    eval(script.textContent || 'console.log("Script is external")');
  }
} catch (e) {
  console.error('Syntax error:', e);
}
```

---

## 📝 Альтернативное решение

Если проблема сохраняется, можно временно добавить версию к URL скрипта в `strategy-builder.html`:

```html
<script src="./js/pages/strategy_builder.js?v=20260129_2"></script>
```

Это заставит браузер загрузить новую версию файла.

---

**Статус**: ✅ Файл исправлен на сервере  
**Требуется**: Очистка кэша браузера и перезагрузка страницы
