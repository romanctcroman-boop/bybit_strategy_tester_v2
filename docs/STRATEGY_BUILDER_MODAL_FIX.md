# 🔧 Strategy Builder - Исправление модального окна Templates

> **Дата**: 2026-01-29  
> **Проблема**: Модальное окно Templates открывается и сразу закрывается

---

## 🔍 Диагностика проблемы

### Обнаружено из логов:

1. ✅ Кнопка Templates найдена и видима
2. ✅ Обработчик события привязан
3. ✅ При клике вызывается `openTemplatesModal()`
4. ✅ Модальное окно открывается (`Templates modal opened`)
5. ❌ Модальное окно сразу закрывается (`Templates modal closed`)

**Причина**: Модальное окно закрывается из-за клика на overlay или конфликта обработчиков событий.

---

## ✅ Исправления применены

### 1. Улучшена обработка кликов по модальному окну

- Добавлен `stopPropagation()` для предотвращения закрытия при клике на содержимое модального окна
- Улучшена проверка кликов на overlay vs содержимое

### 2. Исправлен CSS для модального окна

- Добавлен `pointer-events: none` для неактивного состояния
- Добавлен `pointer-events: all` для активного состояния
- Добавлена плавная анимация появления (`opacity` transition)

### 3. Улучшена функция открытия модального окна

- Добавлено логирование состояния модального окна
- Добавлена проверка, открыто ли уже модальное окно (toggle функциональность)
- Улучшена функция `renderTemplates()` с проверкой наличия шаблонов

### 4. Улучшена функция выбора шаблона

- Добавлено визуальное выделение выбранного шаблона
- Добавлено логирование выбора

---

## 🧪 Как проверить исправления

### Шаг 1: Перезагрузите страницу

```
Ctrl+F5 (для очистки кеша)
```

### Шаг 2: Откройте консоль браузера (F12)

### Шаг 3: Нажмите кнопку Templates

Должны появиться логи:
```
[Strategy Builder] Templates button clicked
[Strategy Builder] Opening modal
[Strategy Builder] Opening templates modal
[Strategy Builder] Templates modal opened
[Strategy Builder] Modal classes: modal-overlay active
[Strategy Builder] Modal display: flex
[Strategy Builder] Rendering templates, count: 7
[Strategy Builder] Templates rendered, HTML length: 5537, Templates in DOM: 7
```

### Шаг 4: Проверьте модальное окно

Модальное окно должно:
- ✅ Открыться и остаться открытым
- ✅ Показать 7 шаблонов (включая "RSI Long then Short")
- ✅ Не закрываться при клике на содержимое
- ✅ Закрываться при клике на overlay (темный фон)
- ✅ Закрываться при клике на кнопку Close/Cancel

---

## 🐛 Если модальное окно все еще не работает

### Проверка в консоли:

```javascript
// Проверить модальное окно
const modal = document.getElementById("templatesModal");
console.log("Modal:", modal);
console.log("Has active class:", modal?.classList.contains("active"));
console.log("Display:", window.getComputedStyle(modal).display);
console.log("Z-index:", window.getComputedStyle(modal).zIndex);

// Открыть вручную
if (modal) {
  modal.classList.add("active");
  console.log("Modal opened manually");
}

// Проверить шаблоны
const templatesGrid = document.getElementById("templatesGrid");
console.log("Templates grid:", templatesGrid);
console.log("Templates count:", templatesGrid?.querySelectorAll(".template-card").length);
```

### Проверка шаблонов:

```javascript
// Проверить, что шаблоны определены
console.log("Templates defined:", typeof templates !== 'undefined');
console.log("Templates count:", templates?.length);

// Проверить новый шаблон
const rsiTemplate = templates?.find(t => t.id === 'rsi_long_short');
console.log("RSI template:", rsiTemplate);
```

---

## 📝 Измененные файлы

1. `frontend/js/pages/strategy_builder.js`
   - Улучшена функция `openTemplatesModal()`
   - Исправлена обработка кликов по модальному окну
   - Улучшена функция `selectTemplate()`
   - Улучшена функция `renderTemplates()`

2. `frontend/css/strategy_builder.css`
   - Исправлен CSS для модального окна
   - Добавлены `pointer-events` для правильной работы

---

**Статус**: ✅ Исправления применены  
**Требуется**: Перезагрузка страницы и проверка работы модального окна
