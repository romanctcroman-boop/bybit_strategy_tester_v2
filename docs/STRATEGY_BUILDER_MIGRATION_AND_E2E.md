# 🚀 Strategy Builder - Применение миграции и E2E тестирование

> **Дата**: 2026-01-29  
> **Статус**: Готово к выполнению

---

## 📋 Шаг 1: Применение миграции БД

### Вариант A: Через скрипт (рекомендуется)

```powershell
# Из корневой директории проекта
py -3.14 scripts/apply_strategy_builder_migration.py
```

### Вариант B: Через Alembic напрямую

```powershell
# Проверить текущую версию миграции
py -3.14 -m alembic current

# Применить миграцию до head
py -3.14 -m alembic upgrade head

# Проверить, что миграция применилась
py -3.14 -m alembic current
```

### Ожидаемый результат

После применения миграции в таблице `strategies` должны появиться поля:
- `builder_graph` (JSON, nullable)
- `builder_blocks` (JSON, nullable)
- `builder_connections` (JSON, nullable)
- `is_builder_strategy` (Boolean, default False)
- Индекс `ix_strategies_is_builder_strategy`

### Проверка миграции

Можно проверить через SQL:

```sql
-- Проверить структуру таблицы
PRAGMA table_info(strategies);

-- Или через Python
python -c "from backend.database.session import SessionLocal; from backend.database.models.strategy import Strategy; db = SessionLocal(); print([c.name for c in Strategy.__table__.columns])"
```

---

## 🧪 Шаг 2: E2E тестирование полного flow

### Запуск E2E тестов

```powershell
# Из корневой директории проекта
py -3.14 scripts/strategy_builder_e2e_test.py
```

### Что тестируется

Скрипт `strategy_builder_e2e_test.py` проверяет:

1. **✅ Создание стратегии через API**
   - POST `/api/v1/strategy-builder/strategies`
   - Проверка: стратегия создана, ID возвращен, `is_builder_strategy=True`

2. **✅ Загрузка стратегии**
   - GET `/api/v1/strategy-builder/strategies/{id}`
   - Проверка: все блоки и соединения загружены корректно

3. **✅ Обновление стратегии**
   - PUT `/api/v1/strategy-builder/strategies/{id}`
   - Проверка: изменения сохранены

4. **✅ Генерация кода**
   - POST `/api/v1/strategy-builder/strategies/{id}/generate-code`
   - Проверка: Python код сгенерирован без ошибок

5. **✅ Запуск бэктеста**
   - POST `/api/v1/strategy-builder/strategies/{id}/backtest`
   - Проверка: бэктест запущен (может использовать синтетические данные)

6. **✅ Список стратегий**
   - GET `/api/v1/strategy-builder/strategies`
   - Проверка: пагинация работает, фильтрация по `is_builder_strategy`

7. **✅ Удаление стратегии (soft delete)**
   - DELETE `/api/v1/strategy-builder/strategies/{id}`
   - Проверка: стратегия помечена как удаленная, недоступна через GET

### Ожидаемый вывод

```
============================================================
STRATEGY BUILDER E2E ТЕСТЫ
============================================================
Время начала: 2026-01-29T...

============================================================
ТЕСТ 1: Создание стратегии через API
============================================================
✅ Стратегия создана: ID = <uuid>
   Название: E2E Test RSI Strategy
   is_builder_strategy: True

============================================================
ТЕСТ 2: Загрузка стратегии из БД
============================================================
✅ Стратегия загружена: ID = <uuid>
   Блоков: 5
   Соединений: 4
   Название: E2E Test RSI Strategy

... (остальные тесты)

============================================================
✅ ВСЕ E2E ТЕСТЫ ПРОШЛИ УСПЕШНО!
============================================================
```

---

## 🌐 Шаг 3: Ручное тестирование через UI

После успешного прохождения автоматических тестов, протестируйте через браузер:

### 1. Создание стратегии

1. Откройте `http://localhost:8000/frontend/strategy-builder.html`
2. Добавьте блоки:
   - Price (input)
   - RSI (indicator, period=14)
   - Constant (value=30)
   - Less Than (condition)
3. Соедините блоки:
   - Price → RSI (source)
   - RSI → Less Than (a)
   - Constant → Less Than (b)
   - Less Than → Strategy (entry_long)
4. Нажмите "Save"
5. ✅ Проверка: URL обновился с `?id=<uuid>`, появилось уведомление "Strategy saved"

### 2. Загрузка стратегии

1. Скопируйте ID из URL
2. Откройте новую вкладку: `http://localhost:8000/frontend/strategy-builder.html?id=<uuid>`
3. ✅ Проверка: все блоки и соединения отображаются корректно

### 3. Запуск бэктеста

1. Нажмите кнопку "Backtest"
2. Заполните параметры (или используйте значения по умолчанию)
3. Нажмите "Run Backtest"
4. ✅ Проверка: редирект на страницу результатов, бэктест выполнен

### 4. Генерация кода

1. Нажмите кнопку "Generate Code"
2. ✅ Проверка: открылось новое окно с Python кодом, код валидный

### 5. Автосохранение

1. Измените название стратегии или параметры блоков
2. Подождите 30 секунд
3. ✅ Проверка: надпись "Last saved" обновилась, изменения сохранены в БД

---

## 🔍 Проверка логов

### Backend логи

Проверьте логи backend на наличие ошибок:

```powershell
# Если backend запущен через uvicorn
# Логи должны быть в консоли или в файле логов
```

Ожидаемые сообщения:
- `INFO: Strategy created: <id>`
- `INFO: Strategy loaded: <id>`
- `INFO: Backtest started: <backtest_id>`
- `INFO: Code generated for strategy: <id>`

### Frontend логи

Откройте DevTools (F12) в браузере и проверьте:
- Нет ошибок в Console
- Нет ошибок в Network (все запросы возвращают 200/201)

---

## ❌ Устранение проблем

### Проблема: Миграция не применяется

**Решение**:
1. Проверьте, что вы в корневой директории проекта
2. Проверьте, что база данных существует и доступна
3. Проверьте текущую версию: `py -3.14 -m alembic current`
4. Если миграция уже применена, вы увидите: `20260129_strategy_builder (head)`

### Проблема: E2E тесты падают

**Решение**:
1. Убедитесь, что миграция применена
2. Проверьте, что все зависимости установлены: `pip install -r requirements.txt`
3. Проверьте логи ошибок в выводе теста
4. Убедитесь, что backend не запущен (тесты используют in-memory БД)

### Проблема: UI не работает

**Решение**:
1. Убедитесь, что backend запущен: `py -3.14 scripts/start_uvicorn.ps1`
2. Проверьте, что frontend доступен: `http://localhost:8000/frontend/strategy-builder.html`
3. Проверьте консоль браузера на ошибки
4. Проверьте Network tab в DevTools - все запросы должны быть успешными

---

## ✅ Критерии успешного завершения

После выполнения всех шагов должны быть выполнены:

- [x] Миграция применена успешно
- [x] Все E2E тесты проходят (7/7)
- [x] Стратегия создается через UI
- [x] Стратегия загружается из БД
- [x] Бэктест запускается и завершается успешно
- [x] Код генерируется без ошибок
- [x] Автосохранение работает (обновление каждые 30 сек)
- [x] Нет ошибок в логах backend
- [x] Нет ошибок в консоли браузера

---

## 📝 Следующие шаги

После успешного завершения тестирования:

1. **Расширение поддержки блоков** - добавить больше индикаторов и условий
2. **Улучшение валидации** - проверка совместимости портов, отсутствие циклов
3. **Версионирование** - сохранение истории изменений стратегий
4. **Шаблоны** - предустановленные стратегии для быстрого старта

---

**Готово к выполнению!** 🚀
