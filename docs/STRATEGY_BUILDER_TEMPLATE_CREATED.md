# ✅ Strategy Builder - Шаблон "RSI Long then Short" создан

> **Дата**: 2026-01-29  
> **Шаблон ID**: `rsi_long_short`  
> **Статус**: ✅ Готов к использованию

---

## 📊 Описание шаблона

### Логика стратегии:

**RSI Long then Short** - стратегия на основе индикатора RSI:

1. **Long Entry**: Когда RSI < 30 (перепроданность)
2. **Long Exit**: Когда RSI > 70 (перекупленность)
3. **Short Entry**: Когда RSI > 70 (перекупленность)
4. **Short Exit**: Когда RSI < 30 (перепроданность)

### Параметры:

- **RSI Period**: 14
- **RSI Overbought**: 70
- **RSI Oversold**: 30

---

## 🧩 Структура шаблона

### Блоки (5 блоков + main_strategy):

1. **RSI** (indicator)
   - Параметры: period=14, overbought=70, oversold=30
   - Позиция: (150, 150)

2. **Constant 30** (input)
   - Значение: 30
   - Позиция: (150, 300)

3. **Constant 70** (input)
   - Значение: 70
   - Позиция: (150, 450)

4. **Less Than** (condition)
   - Проверяет: RSI < 30
   - Позиция: (400, 200)

5. **Greater Than** (condition)
   - Проверяет: RSI > 70
   - Позиция: (400, 400)

6. **Strategy** (main node)
   - Главный узел стратегии
   - Позиция: (600, 250)

### Соединения (8 соединений):

**Data connections (4):**
1. **RSI.value → Less Than.left** - RSI значение для сравнения
2. **Constant 30.value → Less Than.right** - Порог oversold (30)
3. **RSI.value → Greater Than.left** - RSI значение для сравнения
4. **Constant 70.value → Greater Than.right** - Порог overbought (70)

**Condition connections (4):**
5. **Less Than.result → Strategy.entry_long** - Вход в Long при RSI < 30
6. **Greater Than.result → Strategy.exit_long** - Выход из Long при RSI > 70
7. **Greater Than.result → Strategy.entry_short** - Вход в Short при RSI > 70
8. **Less Than.result → Strategy.exit_short** - Выход из Short при RSI < 30

---

## 🧪 Как использовать шаблон

### Шаг 1: Открыть Templates Modal

1. Откройте Strategy Builder
2. Нажмите кнопку **Templates** в верхней панели
3. Должно открыться модальное окно с шаблонами

### Шаг 2: Выбрать шаблон

1. Найдите шаблон **"RSI Long then Short"**
2. Кликните на карточку шаблона
3. Шаблон должен выделиться (синяя рамка)

### Шаг 3: Загрузить шаблон

1. Нажмите кнопку **Use Template**
2. Шаблон должен загрузиться на canvas
3. Все блоки и соединения должны появиться

### Шаг 4: Проверить структуру

После загрузки на canvas должны быть:
- ✅ RSI блок с параметрами (period=14, overbought=70, oversold=30)
- ✅ 2 Constant блока (30 и 70)
- ✅ 2 Condition блока (Less Than и Greater Than)
- ✅ Strategy блок (main node)
- ✅ 8 соединений между блоками

### Шаг 5: Сохранить и протестировать

1. Введите название: "RSI Long Short Strategy"
2. Нажмите **Save**
3. Нажмите **Validate** для проверки
4. Нажмите **Generate Code** для генерации Python кода
5. Нажмите **Backtest** для запуска бэктеста

---

## 📝 Тесты для шаблона

Создан файл `tests/backend/services/test_rsi_long_short_template.py` с тестами:

1. **test_template_creation** - Проверка создания стратегии из шаблона
2. **test_template_code_generation** - Проверка генерации кода
3. **test_template_validation** - Проверка валидации
4. **test_template_backtest** - Проверка запуска бэктеста

### Запуск тестов:

```bash
py -3.14 -m pytest tests/backend/services/test_rsi_long_short_template.py -v
```

---

## 🔧 Исправления применены

1. ✅ Исправлены названия портов для условий (`a`/`b` → `left`/`right`)
2. ✅ Улучшена функция `loadTemplateData()` для правильной обработки `main_strategy`
3. ✅ Добавлено логирование при загрузке шаблона
4. ✅ Исправлена обработка соединений с `main_strategy` узлом

---

## 📊 Ожидаемое поведение

### Входы в позиции:

- **Long Entry**: Когда RSI падает ниже 30 (перепроданность)
- **Short Entry**: Когда RSI поднимается выше 70 (перекупленность)

### Выходы из позиций:

- **Long Exit**: Когда RSI поднимается выше 70 (перекупленность)
- **Short Exit**: Когда RSI падает ниже 30 (перепроданность)

### Особенности:

- Стратегия работает в обоих направлениях (Long и Short)
- Использует классические уровни RSI (30/70)
- Подходит для боковых трендов и волатильных рынков
- Простая и понятная логика

---

## 🚀 Готово к использованию

Шаблон создан и готов к использованию:

1. ✅ Добавлен в список шаблонов
2. ✅ Структура блоков определена
3. ✅ Соединения настроены
4. ✅ Тесты созданы
5. ✅ Документация написана

**Следующий шаг**: Загрузить шаблон через UI и протестировать!

---

**Статус**: ✅ **TEMPLATE CREATED**  
**Готов к тестированию**: Да
