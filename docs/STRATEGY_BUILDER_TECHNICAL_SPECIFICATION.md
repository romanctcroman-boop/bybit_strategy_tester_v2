# 📋 Strategy Builder - Техническое Задание (ТЗ)

## Bybit Strategy Tester v2 - Visual Strategy Builder

> **Версия**: 1.0.0  
> **Дата создания**: 2026-01-29  
> **Статус**: Утверждено к реализации

---

## 📑 Содержание

1. [Общее описание](#общее-описание)
2. [Цели и задачи](#цели-и-задачи)
3. [Требования к системе](#требования-к-системе)
4. [Архитектура решения](#архитектура-решения)
5. [Детальный план реализации](#детальный-план-реализации)
6. [Критерии приемки](#критерии-приемки)
7. [Риски и ограничения](#риски-и-ограничения)

---

## Общее описание

### Назначение
Strategy Builder — визуальный конструктор торговых стратегий на основе блоков (drag-and-drop), позволяющий пользователям создавать стратегии без написания кода.

### Целевая аудитория
- Трейдеры без навыков программирования
- Разработчики стратегий (быстрое прототипирование)
- AI агенты (DeepSeek, Perplexity) для автоматического создания стратегий

### Основные возможности
1. **Визуальное создание стратегий** — перетаскивание блоков на canvas
2. **Автоматическая генерация кода** — Python код из визуального графа
3. **Интеграция с бэктестингом** — запуск бэктестов прямо из конструктора
4. **Сохранение/загрузка** — персистентность в БД
5. **Шаблоны стратегий** — готовые шаблоны для быстрого старта
6. **Валидация** — проверка корректности стратегии перед запуском

---

## Цели и задачи

### Основные цели
1. ✅ Упростить создание торговых стратегий для непрограммистов
2. ✅ Интегрировать с существующей инфраструктурой бэктестинга
3. ✅ Обеспечить поддержку AI агентов
4. ✅ Сохранять стратегии в БД для последующего использования

### Задачи
- [x] Создать техническую документацию
- [x] Расширить модель БД для хранения graph данных
- [x] Реализовать REST API для CRUD операций
- [ ] Интегрировать frontend с новым API
- [ ] Реализовать генерацию Python кода из блоков
- [ ] Создать адаптер для преобразования graph в BaseStrategy
- [ ] Интегрировать с движками бэктестинга
- [ ] Добавить автосохранение и версионирование

---

## Требования к системе

### Функциональные требования

#### FR-1: Создание стратегии
- **Описание**: Пользователь может создать новую стратегию через UI
- **Входные данные**: Название, описание, timeframe, symbol, market_type, direction
- **Выходные данные**: ID созданной стратегии
- **Приоритет**: Высокий

#### FR-2: Добавление блоков
- **Описание**: Пользователь может добавлять блоки на canvas (drag-and-drop или клик)
- **Типы блоков**: Indicators, Conditions, Actions, Logic, Inputs
- **Приоритет**: Высокий

#### FR-3: Соединение блоков
- **Описание**: Пользователь может соединять блоки через порты
- **Валидация**: Проверка совместимости типов портов (data/condition/flow)
- **Приоритет**: Высокий

#### FR-4: Сохранение стратегии
- **Описание**: Стратегия сохраняется в БД с полным graph (blocks + connections)
- **Формат**: JSON в полях `builder_graph`, `builder_blocks`, `builder_connections`
- **Приоритет**: Высокий

#### FR-5: Загрузка стратегии
- **Описание**: Стратегия загружается из БД и отображается на canvas
- **Источники**: БД по ID, URL параметр `?id=...`
- **Приоритет**: Высокий

#### FR-6: Валидация стратегии
- **Описание**: Проверка корректности стратегии перед запуском бэктеста
- **Проверки**:
  - Наличие блоков
  - Подключение entry сигналов к main_strategy node
  - Совместимость типов портов
  - Отсутствие циклов (кроме разрешенных)
- **Приоритет**: Высокий

#### FR-7: Генерация кода
- **Описание**: Автоматическая генерация Python кода из визуального графа
- **Формат**: Python функция `generate_signals(ohlcv: pd.DataFrame) -> dict`
- **Приоритет**: Средний

#### FR-8: Запуск бэктеста
- **Описание**: Запуск бэктеста из Strategy Builder
- **Интеграция**: Преобразование graph → BaseStrategy → BacktestEngine
- **Приоритет**: Высокий

#### FR-9: Автосохранение
- **Описание**: Автоматическое сохранение изменений
- **Механизм**: localStorage (мгновенно) + периодический PATCH на сервер (каждые 30 сек)
- **Приоритет**: Средний

#### FR-10: Версионирование
- **Описание**: Сохранение версий стратегий для отката изменений
- **Механизм**: Поле `version` в БД + история изменений
- **Приоритет**: Низкий

### Нефункциональные требования

#### NFR-1: Производительность
- Загрузка стратегии: < 500ms
- Сохранение стратегии: < 1s
- Валидация: < 200ms
- Генерация кода: < 2s

#### NFR-2: Масштабируемость
- Поддержка до 100 блоков на стратегию
- Поддержка до 200 соединений
- Поддержка до 1000 стратегий в БД

#### NFR-3: Совместимость
- Работа с существующими движками бэктестинга (FallbackV2/V3/V4, NumbaV2, GPUV2)
- Совместимость с TradingView форматом данных (SPOT market_type)
- Поддержка всех существующих индикаторов

#### NFR-4: Безопасность
- Валидация всех входных данных
- Защита от SQL injection (SQLAlchemy ORM)
- Защита от XSS (санитизация JSON)

---

## Архитектура решения

### Компоненты системы

```
┌─────────────────────────────────────────────────────────────┐
│                    FRONTEND LAYER                           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Block Library│  │   Canvas    │  │  Properties  │     │
│  │  (Sidebar)   │  │  (Graph)    │  │   Panel      │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│  strategy-builder.html + strategy_builder.js                │
└─────────────────────────────┬─────────────────────────────┘
                              │ REST API (JSON)
┌─────────────────────────────▼─────────────────────────────┐
│                  API LAYER (FastAPI)                      │
│  /api/v1/strategy-builder/*                              │
│  ┌────────────────────────────────────────────────────┐   │
│  │  Strategy Builder Router                          │   │
│  │  - CRUD strategies                                │   │
│  │  - Block operations                               │   │
│  │  - Connection management                          │   │
│  │  - Validation                                     │   │
│  │  - Code generation                               │   │
│  │  - Backtest integration                          │   │
│  └────────────────────────────────────────────────────┘   │
└─────────────────────────────┬─────────────────────────────┘
                              │
┌─────────────────────────────▼─────────────────────────────┐
│              SERVICE LAYER                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │ StrategyGraph│  │ CodeGenerator│  │ Validator   │   │
│  │   Builder    │  │              │  │             │   │
│  └──────────────┘  └──────────────┘  └──────────────┘   │
│  ┌──────────────┐  ┌──────────────┐                    │
│  │StrategyBuilder│  │TemplateManager│                   │
│  │   Adapter    │  │              │                    │
│  └──────────────┘  └──────────────┘                    │
└─────────────────────────────┬─────────────────────────────┘
                              │
┌─────────────────────────────▼─────────────────────────────┐
│            PERSISTENCE LAYER                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐   │
│  │  Strategy    │  │  Backtest    │  │  Templates  │   │
│  │   Model      │  │   Model      │  │   Storage   │   │
│  └──────────────┘  └──────────────┘  └──────────────┘   │
│  SQLite/PostgreSQL                                        │
└───────────────────────────────────────────────────────────┘
```

### Формат данных

#### Strategy Graph JSON
```json
{
  "id": "strategy_abc123",
  "name": "My RSI Strategy",
  "description": "RSI oversold/overbought",
  "timeframe": "1h",
  "symbol": "BTCUSDT",
  "market_type": "linear",
  "direction": "both",
  "initial_capital": 10000,
  "blocks": [
    {
      "id": "block_rsi",
      "type": "rsi",
      "category": "indicator",
      "name": "RSI",
      "icon": "graph-up",
      "x": 100,
      "y": 200,
      "params": {"period": 14, "overbought": 70, "oversold": 30}
    }
  ],
  "connections": [
    {
      "id": "conn_1",
      "source": {"blockId": "block_rsi", "portId": "value"},
      "target": {"blockId": "block_less_than", "portId": "a"},
      "type": "data"
    }
  ]
}
```

---

## Детальный план реализации

### Этап 1: Инфраструктура БД ✅ (Завершен)

#### Задача 1.1: Расширение модели Strategy
- [x] Добавить поле `builder_graph` (JSON)
- [x] Добавить поле `builder_blocks` (JSON)
- [x] Добавить поле `builder_connections` (JSON)
- [x] Добавить поле `is_builder_strategy` (Boolean)
- [x] Обновить метод `to_dict()`

**Критерии приемки**:
- Модель компилируется без ошибок
- Все поля имеют правильные типы
- Метод `to_dict()` возвращает все поля

#### Задача 1.2: Создание миграции БД
- [ ] Создать Alembic migration для новых полей
- [ ] Протестировать миграцию up/down
- [ ] Добавить индексы для `is_builder_strategy`

**Критерии приемки**:
- Миграция применяется без ошибок
- Откат миграции работает корректно
- Индексы созданы

---

### Этап 2: Backend API ✅ (Завершен)

#### Задача 2.1: CRUD эндпоинты
- [x] `POST /api/v1/strategy-builder/strategies` - Создание
- [x] `GET /api/v1/strategy-builder/strategies/{id}` - Получение
- [x] `PUT /api/v1/strategy-builder/strategies/{id}` - Обновление
- [x] `DELETE /api/v1/strategy-builder/strategies/{id}` - Удаление
- [x] `GET /api/v1/strategy-builder/strategies` - Список с пагинацией

**Критерии приемки**:
- Все эндпоинты возвращают правильные коды статуса
- Данные сохраняются в БД корректно
- Валидация входных данных работает

#### Задача 2.2: Эндпоинт бэктеста
- [x] `POST /api/v1/strategy-builder/strategies/{id}/backtest` - Заглушка
- [ ] Полная реализация с StrategyBuilderAdapter

**Критерии приемки**:
- Эндпоинт принимает параметры бэктеста
- Валидирует стратегию перед запуском
- Возвращает backtest_id или ошибку

---

### Этап 3: Frontend Integration 🔄 (В процессе)

#### Задача 3.1: Обновление saveStrategy()
**Файл**: `frontend/js/pages/strategy_builder.js`

**Текущая реализация**:
```javascript
async function saveStrategy() {
  const strategy = {
    name: document.getElementById("strategyName").value,
    // ... собирает данные
  };
  
  const response = await fetch("/api/strategy-builder/strategies", {
    method: "POST",
    // ...
  });
}
```

**Новая реализация**:
```javascript
async function saveStrategy() {
  const strategy = {
    name: document.getElementById("strategyName").value,
    description: "", // TODO: добавить поле описания
    timeframe: document.getElementById("strategyTimeframe").value,
    symbol: document.getElementById("strategySymbol").value,
    market_type: document.getElementById("builderMarketType").value,
    direction: document.getElementById("builderDirection").value,
    initial_capital: parseFloat(document.getElementById("initialCapital").value),
    blocks: strategyBlocks,
    connections: connections,
  };

  try {
    const strategyId = getStrategyIdFromURL(); // Из ?id=...
    const method = strategyId ? "PUT" : "POST";
    const url = strategyId 
      ? `/api/v1/strategy-builder/strategies/${strategyId}`
      : `/api/v1/strategy-builder/strategies`;

    const response = await fetch(url, {
      method: method,
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(strategy),
    });

    if (response.ok) {
      const data = await response.json();
      updateLastSaved();
      showNotification("Strategy saved successfully!", "success");
      
      // Обновить URL если новая стратегия
      if (!strategyId) {
        window.history.pushState({}, "", `?id=${data.id}`);
      }
    } else {
      const error = await response.json();
      showNotification(`Error: ${error.detail}`, "error");
    }
  } catch (err) {
    showNotification("Failed to save strategy", "error");
  }
}
```

**Критерии приемки**:
- Стратегия сохраняется в БД
- URL обновляется при создании новой стратегии
- Показываются уведомления об успехе/ошибке
- Обновляется "Last saved" timestamp

#### Задача 3.2: Обновление loadStrategy()
**Новая реализация**:
```javascript
async function loadStrategy(strategyId) {
  try {
    const response = await fetch(`/api/v1/strategy-builder/strategies/${strategyId}`);
    if (!response.ok) {
      throw new Error(`HTTP ${response.status}`);
    }
    
    const strategy = await response.json();
    
    // Обновить UI поля
    document.getElementById("strategyName").value = strategy.name;
    document.getElementById("strategyTimeframe").value = strategy.timeframe;
    document.getElementById("strategySymbol").value = strategy.symbol;
    document.getElementById("builderMarketType").value = strategy.market_type;
    document.getElementById("builderDirection").value = strategy.direction;
    document.getElementById("initialCapital").value = strategy.initial_capital;
    
    // Восстановить блоки и соединения
    strategyBlocks = strategy.blocks || [];
    connections.length = 0;
    if (strategy.connections) {
      connections.push(...strategy.connections);
    }
    
    // Перерисовать canvas
    renderBlocks();
    renderConnections();
    
    updateLastSaved(strategy.updated_at);
    showNotification("Strategy loaded successfully!", "success");
  } catch (err) {
    showNotification(`Failed to load strategy: ${err.message}`, "error");
  }
}

// Загрузка при открытии страницы если есть ?id=
document.addEventListener("DOMContentLoaded", () => {
  const urlParams = new URLSearchParams(window.location.search);
  const strategyId = urlParams.get("id");
  if (strategyId) {
    loadStrategy(strategyId);
  } else {
    // Создать новый main_strategy node
    createMainStrategyNode();
  }
});
```

**Критерии приемки**:
- Стратегия загружается из БД
- Все блоки и соединения отображаются на canvas
- UI поля заполняются корректно
- Обрабатываются ошибки загрузки

#### Задача 3.3: Обновление runBacktest()
**Новая реализация**:
```javascript
async function runBacktest() {
  const strategyId = getStrategyIdFromURL();
  if (!strategyId) {
    showNotification("Please save strategy before running backtest", "warning");
    return;
  }

  // Собрать параметры бэктеста из UI
  const backtestParams = {
    start_date: document.getElementById("backtestStartDate")?.value || "2024-01-01T00:00:00Z",
    end_date: document.getElementById("backtestEndDate")?.value || "2024-12-31T23:59:59Z",
    engine: document.getElementById("backtestEngine")?.value || null,
    commission: parseFloat(document.getElementById("backtestCommission")?.value || 0.0007),
    slippage: parseFloat(document.getElementById("backtestSlippage")?.value || 0.0005),
    leverage: parseInt(document.getElementById("backtestLeverage")?.value || 10),
    pyramiding: parseInt(document.getElementById("backtestPyramiding")?.value || 1),
    stop_loss: parseFloat(document.getElementById("backtestStopLoss")?.value || null),
    take_profit: parseFloat(document.getElementById("backtestTakeProfit")?.value || null),
  };

  try {
    const response = await fetch(`/api/v1/strategy-builder/strategies/${strategyId}/backtest`, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(backtestParams),
    });

    if (response.ok) {
      const data = await response.json();
      
      if (data.backtest_id) {
        // Редирект на страницу результатов
        window.location.href = `/frontend/backtest-results.html?backtest_id=${data.backtest_id}`;
      } else {
        showNotification("Backtest started. Check results later.", "info");
      }
    } else {
      const error = await response.json();
      showNotification(`Backtest failed: ${error.detail}`, "error");
    }
  } catch (err) {
    showNotification(`Failed to start backtest: ${err.message}`, "error");
  }
}
```

**Критерии приемки**:
- Бэктест запускается через API
- Показываются уведомления о статусе
- Редирект на страницу результатов работает

#### Задача 3.4: Автосохранение
**Реализация**:
```javascript
let autoSaveTimer = null;
let lastSavedHash = null;

function getStrategyHash() {
  return JSON.stringify({
    blocks: strategyBlocks,
    connections: connections,
    name: document.getElementById("strategyName").value,
  });
}

function scheduleAutoSave() {
  // Отменить предыдущий таймер
  if (autoSaveTimer) {
    clearTimeout(autoSaveTimer);
  }

  // Сохранить в localStorage сразу
  const strategyId = getStrategyIdFromURL();
  if (strategyId) {
    const draft = {
      id: strategyId,
      blocks: strategyBlocks,
      connections: connections,
      name: document.getElementById("strategyName").value,
      timestamp: new Date().toISOString(),
    };
    localStorage.setItem(`strategy_draft_${strategyId}`, JSON.stringify(draft));
  }

  // Запланировать сохранение на сервер через 30 сек
  autoSaveTimer = setTimeout(async () => {
    const currentHash = getStrategyHash();
    if (currentHash !== lastSavedHash) {
      await saveStrategy(); // Использует существующую функцию
      lastSavedHash = currentHash;
    }
  }, 30000); // 30 секунд
}

// Вызывать при изменениях
function onStrategyChanged() {
  scheduleAutoSave();
}

// Подписаться на изменения
document.addEventListener("DOMContentLoaded", () => {
  // При изменении блоков
  const originalAddBlock = addBlockToCanvas;
  addBlockToCanvas = function(...args) {
    originalAddBlock.apply(this, args);
    onStrategyChanged();
  };

  // При изменении соединений
  const originalCompleteConnection = completeConnection;
  completeConnection = function(...args) {
    originalCompleteConnection.apply(this, args);
    onStrategyChanged();
  };

  // При изменении параметров блоков
  const originalUpdateParam = updateBlockParam;
  updateBlockParam = function(...args) {
    originalUpdateParam.apply(this, args);
    onStrategyChanged();
  };

  // При изменении названия
  document.getElementById("strategyName").addEventListener("input", onStrategyChanged);
});
```

**Критерии приемки**:
- Изменения сохраняются в localStorage мгновенно
- Периодическое сохранение на сервер работает
- Не создаются дубликаты при автосохранении

---

### Этап 4: Code Generation & Execution ⏳ (Запланировано)

#### Задача 4.1: StrategyBuilderAdapter
**Файл**: `backend/backtesting/strategy_builder_adapter.py`

**Реализация**:
```python
from backend.backtesting.strategies import BaseStrategy, SignalResult
import pandas as pd
import numpy as np

class StrategyBuilderAdapter(BaseStrategy):
    """Adapter для преобразования Strategy Builder graph в BaseStrategy"""
    
    def __init__(self, strategy_graph: dict):
        self.graph = strategy_graph
        self.name = strategy_graph["name"]
        self.description = strategy_graph.get("description", "")
        self.params = self._extract_params(strategy_graph)
        self._compiled_code = None
        
    def _extract_params(self, graph: dict) -> dict:
        """Извлечь параметры из блоков"""
        params = {}
        for block in graph.get("blocks", []):
            if block.get("params"):
                params[block["id"]] = block["params"]
        return params
    
    def generate_signals(self, ohlcv: pd.DataFrame) -> SignalResult:
        """Генерировать сигналы из графа"""
        # TODO: Реализовать выполнение графа
        # 1. Топологическая сортировка блоков
        # 2. Выполнение блоков в порядке зависимостей
        # 3. Сборка сигналов для main_strategy node
        
        # Временная заглушка
        return SignalResult(
            entries=pd.Series([False] * len(ohlcv)),
            exits=pd.Series([False] * len(ohlcv)),
        )
```

**Критерии приемки**:
- Адаптер реализует интерфейс BaseStrategy
- Граф преобразуется в сигналы корректно
- Работает с существующими движками бэктестинга

#### Задача 4.2: Реальная генерация кода
**Файл**: `backend/services/strategy_builder/code_generator.py`

**Требования**:
- Генерация Python кода для всех типов блоков
- Правильная обработка зависимостей
- Валидация сгенерированного кода

**Критерии приемки**:
- Генерируется валидный Python код
- Код выполняется без ошибок
- Результаты совпадают с визуальным графом

---

### Этап 5: Интеграция с бэктестингом ⏳ (Запланировано)

#### Задача 5.1: Полная реализация backtest endpoint
**Файл**: `backend/api/routers/strategy_builder.py`

**Реализация**:
```python
@router.post("/strategies/{strategy_id}/backtest")
async def run_backtest_from_builder(
    strategy_id: str,
    request: BacktestRequest,
    db: Session = Depends(get_db),
):
    # 1. Загрузить стратегию из БД
    # 2. Создать StrategyBuilderAdapter
    # 3. Выбрать движок на основе features стратегии
    # 4. Запустить бэктест через существующий API
    # 5. Вернуть backtest_id и redirect_url
```

**Критерии приемки**:
- Бэктест запускается успешно
- Выбирается правильный движок
- Результаты сохраняются в БД
- Возвращается корректный redirect_url

---

## Критерии приемки (Общие)

### Функциональные
- ✅ Стратегии создаются и сохраняются в БД
- ✅ Стратегии загружаются из БД и отображаются на canvas
- ✅ Блоки можно добавлять, удалять, перемещать
- ✅ Блоки можно соединять через порты
- ✅ Валидация работает корректно
- ✅ Бэктесты запускаются из конструктора
- ✅ AI агенты могут работать с системой

### Производительность
- Загрузка стратегии: < 500ms
- Сохранение стратегии: < 1s
- Валидация: < 200ms
- Генерация кода: < 2s

### Качество кода
- Покрытие тестами: > 80%
- Линтинг: без ошибок (ruff)
- Типизация: mypy без ошибок
- Документация: все публичные методы документированы

---

## Риски и ограничения

### Технические риски
1. **Сложность генерации кода** - Может потребоваться много времени
   - **Митигация**: Начать с простых блоков, постепенно добавлять сложные

2. **Производительность больших графов** - Графы с 100+ блоками могут быть медленными
   - **Митигация**: Оптимизация алгоритмов, кэширование

3. **Совместимость с движками** - Не все фичи могут поддерживаться всеми движками
   - **Митигация**: Автоматический выбор движка на основе features

### Ограничения
- Максимум 100 блоков на стратегию (можно увеличить)
- Максимум 200 соединений (можно увеличить)
- Только Python код (не поддерживаются другие языки)

---

## Приоритеты реализации

### Высокий приоритет (P0)
1. ✅ Расширение модели БД
2. ✅ Backend API (CRUD)
3. 🔄 Frontend интеграция (save/load)
4. ⏳ StrategyBuilderAdapter
5. ⏳ Интеграция с бэктестингом

### Средний приоритет (P1)
6. ⏳ Автосохранение
7. ⏳ Генерация кода
8. ⏳ Валидация стратегий

### Низкий приоритет (P2)
9. ⏳ Версионирование
10. ⏳ Undo/Redo
11. ⏳ Auto-layout
12. ⏳ Шаблоны стратегий

---

## Метрики успеха

- **Использование**: 50+ стратегий создано через Builder за месяц
- **Производительность**: 95% операций выполняются за < 1s
- **Надежность**: < 1% ошибок при сохранении/загрузке
- **Удовлетворенность**: Пользователи могут создавать стратегии без кода

---

**Утверждено**: 2026-01-29  
**Ответственный**: Development Team  
**Статус**: В реализации
