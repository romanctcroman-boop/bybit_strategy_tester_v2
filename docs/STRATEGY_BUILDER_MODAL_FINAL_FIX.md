# 🔧 Strategy Builder - Финальное исправление модального окна Templates

> **Дата**: 2026-01-29  
> **Проблема**: Модальное окно закрывается сразу после открытия

---

## 🔍 Корневая причина

### Проблема:

Клик на кнопку Templates всплывает (bubbles) до overlay модального окна, который только что открылся, и overlay handler закрывает модальное окно.

**Последовательность событий:**
1. Пользователь кликает на кнопку Templates
2. Кнопка открывает модальное окно (`modal.classList.add("active")`)
3. Событие клика всплывает до overlay
4. Overlay handler видит клик на себя и закрывает модальное окно

---

## ✅ Финальное решение

### 1. Использован timestamp для отслеживания открытия

- При открытии модального окна сохраняется timestamp
- Overlay handler игнорирует клики в течение 500ms после открытия
- Это предотвращает закрытие при открытии через кнопку

### 2. Использован флаг открытия через кнопку

- Флаг `openingViaButton` устанавливается при открытии через кнопку
- Overlay handler проверяет этот флаг перед закрытием
- Флаг сбрасывается через 600ms

### 3. Правильный порядок обработчиков

- Кнопка Templates использует bubbling phase с `stopPropagation()`
- Overlay handler использует bubbling phase (не capture)
- Это позволяет кнопке остановить всплытие события раньше

### 4. Улучшен CSS

- Добавлено `visibility: hidden/visible` для правильного отображения
- Улучшена анимация появления

---

## 🧪 Как проверить

### Шаг 1: Перезагрузите страницу

```
Ctrl+F5 (для очистки кеша)
```

### Шаг 2: Откройте консоль браузера (F12)

### Шаг 3: Нажмите кнопку Templates

**Ожидаемые логи:**
```
[Strategy Builder] Templates button clicked
[Strategy Builder] Opening modal via button
[Strategy Builder] Modal open time updated
[Strategy Builder] Opening templates modal
[Strategy Builder] Templates modal opened
[Strategy Builder] Modal classes: modal-overlay active
[Strategy Builder] Modal display: flex
[Strategy Builder] Templates rendered, HTML length: 5537, Templates in DOM: 7
[Strategy Builder] Modal confirmed open
[Strategy Builder] Opening flag reset
```

**НЕ должно быть:**
```
[Strategy Builder] Modal overlay background clicked, closing  ← Этого не должно быть!
```

---

## 🐛 Если проблема осталась

### Временное решение - отключить закрытие по overlay:

Выполните в консоли браузера:

```javascript
// Отключить закрытие по клику на overlay
const modal = document.getElementById("templatesModal");
const handlers = getEventListeners(modal); // Если доступно в DevTools
console.log("Modal handlers:", handlers);

// Открыть модальное окно вручную
modal.classList.add("active");
console.log("Modal opened manually");

// Проверить шаблоны
const grid = document.getElementById("templatesGrid");
const templates = grid.querySelectorAll(".template-card");
console.log("Templates found:", templates.length);
templates.forEach((t, i) => {
  console.log(`${i+1}. ${t.querySelector('.template-name').textContent}`);
});
```

---

## 📝 Измененные файлы

1. `frontend/js/pages/strategy_builder.js`
   - Добавлен timestamp для отслеживания открытия
   - Улучшена проверка флага открытия через кнопку
   - Исправлен порядок обработчиков событий

2. `frontend/css/strategy_builder.css`
   - Добавлено `visibility` для правильного отображения

---

**Статус**: ✅ Финальные исправления применены  
**Требуется**: Перезагрузка страницы и проверка работы модального окна
