# Руководство по Оптимизации Стратегий

**Дата:** 17 октября 2025  
**Версия:** 2.0 (Phase 2 Complete)  
**Статус:** ✅ Production Ready

---

## 📋 Содержание

1. [Введение](#введение)
2. [Grid Search Optimization](#grid-search-optimization)
3. [Walk-Forward Analysis](#walk-forward-analysis)
4. [Bayesian Optimization](#bayesian-optimization)
5. [Сравнение Методов](#сравнение-методов)
6. [Best Practices](#best-practices)
7. [Примеры Использования](#примеры-использования)
8. [Интерпретация Результатов](#интерпретация-результатов)

---

## 🎯 Введение

Система предоставляет три метода оптимизации торговых стратегий:

| Метод            | Скорость | Качество | Сложность | Рекомендуется для                                   |
| ---------------- | -------- | -------- | --------- | --------------------------------------------------- |
| **Grid Search**  | Медленно | Хорошее  | Простая   | Маленькое пространство параметров (<100 комбинаций) |
| **Walk-Forward** | Средне   | Отлично  | Средняя   | Проверка устойчивости параметров                    |
| **Bayesian**     | Быстро   | Отлично  | Средняя   | Большое пространство параметров (>100 комбинаций)   |

---

## 🔲 Grid Search Optimization

### Описание

Grid Search перебирает все возможные комбинации параметров из заданного диапазона.

**Преимущества:**

- ✅ Простой и понятный
- ✅ Гарантированно находит глобальный оптимум (в пределах сетки)
- ✅ Легко интерпретировать результаты

**Недостатки:**

- ❌ Очень медленный при большом количестве параметров
- ❌ Не эффективен для continuous parameters
- ❌ Требует предварительного знания разумных диапазонов

### Когда Использовать

- У вас есть 2-3 параметра
- Диапазоны параметров узкие
- Нужна полная картина пространства параметров
- Время выполнения не критично

### Пример API Request

```python
import requests

url = "http://localhost:8000/api/v1/optimize/grid-search"

request_data = {
    "strategy_class": "MACrossoverStrategy",
    "symbol": "BTCUSDT",
    "timeframe": "1h",
    "start_date": "2024-01-01T00:00:00",
    "end_date": "2024-12-31T23:59:59",
    "parameters": {
        "fast_period": {
            "min": 5,
            "max": 50,
            "step": 5  # Будет проверено: 5, 10, 15, 20, ..., 50 (10 значений)
        },
        "slow_period": {
            "min": 20,
            "max": 200,
            "step": 20  # Будет проверено: 20, 40, 60, ..., 200 (10 значений)
        }
    },
    "initial_capital": 10000.0,
    "commission": 0.001,
    "metric": "sharpe_ratio"
}

# Total combinations: 10 * 10 = 100
response = requests.post(url, json=request_data)
task_id = response.json()["task_id"]
```

---

## 🚶 Walk-Forward Analysis

### Описание

Walk-Forward анализ разбивает исторические данные на скользящие окна:

- **In-Sample (IS)** период - оптимизация параметров
- **Out-of-Sample (OOS)** период - проверка найденных параметров

Процесс:

1. Оптимизировать на IS (например, 120 дней)
2. Тестировать на OOS (например, 60 дней)
3. Сдвинуть окно (например, на 30 дней)
4. Повторить

**Преимущества:**

- ✅ Проверяет устойчивость параметров во времени
- ✅ Предотвращает overfitting
- ✅ Более реалистичные результаты
- ✅ Показывает деградацию производительности

**Недостатки:**

- ❌ Медленнее Grid Search (N окон × Grid Search)
- ❌ Требует больше исторических данных
- ❌ Сложнее интерпретировать

### Когда Использовать

- Проверка реальной производительности стратегии
- Подготовка к live trading
- Оценка стабильности параметров
- Есть достаточно исторических данных (>6 месяцев)

### Пример API Request

```python
url = "http://localhost:8000/api/v1/optimize/walk-forward"

request_data = {
    "strategy_class": "MACrossoverStrategy",
    "symbol": "BTCUSDT",
    "timeframe": "1h",
    "start_date": "2024-01-01T00:00:00",
    "end_date": "2024-12-31T23:59:59",
    "parameters": {
        "fast_period": {
            "min": 5,
            "max": 50,
            "step": 5
        },
        "slow_period": {
            "min": 20,
            "max": 200,
            "step": 20
        }
    },
    "in_sample_period": 120,  # 120 дней для обучения
    "out_sample_period": 60,  # 60 дней для тестирования
    "initial_capital": 10000.0,
    "commission": 0.001,
    "metric": "sharpe_ratio"
}

response = requests.post(url, json=request_data)
task_id = response.json()["task_id"]
```

### Интерпретация Walk-Forward Результатов

Ключевые метрики:

- **positive_window_rate**: % окон с положительной прибылью (должно быть >50%)
- **total_oos_profit**: Общая прибыль на всех OOS периодах
- **std_oos_profit**: Стабильность результатов (меньше = лучше)
- **average_oos_sharpe**: Средний Sharpe ratio на OOS

⚠️ **Важно:** Если positive_window_rate < 50%, стратегия не устойчива!

---

## 🧠 Bayesian Optimization

### Описание

Bayesian optimization использует **Tree-structured Parzen Estimator (TPE)** алгоритм для умного поиска оптимальных параметров.

**Как Работает:**

1. Начинает с random trials
2. Строит вероятностную модель (какие параметры лучше)
3. Выбирает следующие параметры, которые с наибольшей вероятностью улучшат результат
4. Обновляет модель и повторяет

**Преимущества:**

- ✅ Значительно быстрее Grid Search (10-50x)
- ✅ Умно использует предыдущие попытки
- ✅ Работает с любым типом параметров (int, float, categorical)
- ✅ Автоматически вычисляет важность параметров
- ✅ Поддерживает логарифмическую шкалу

**Недостатки:**

- ❌ Не гарантирует глобальный оптимум
- ❌ Результаты могут варьироваться между запусками
- ❌ Сложнее понять процесс оптимизации

### Когда Использовать

- Большое пространство параметров (>100 комбинаций)
- Continuous parameters (float)
- Нужно быстро найти хорошие параметры
- Ограничено время/ресурсы

### Пример API Request

```python
url = "http://localhost:8000/api/v1/optimize/bayesian"

request_data = {
    "strategy_class": "MACrossoverStrategy",
    "symbol": "BTCUSDT",
    "timeframe": "1h",
    "start_date": "2024-01-01T00:00:00",
    "end_date": "2024-12-31T23:59:59",
    "parameters": {
        "fast_period": {
            "type": "int",
            "low": 5,
            "high": 50
        },
        "slow_period": {
            "type": "int",
            "low": 20,
            "high": 200
        },
        "threshold": {
            "type": "float",
            "low": 0.001,
            "high": 0.1,
            "log": True  # Логарифмическая шкала
        },
        "exit_strategy": {
            "type": "categorical",
            "choices": ["fixed", "trailing", "adaptive"]
        }
    },
    "n_trials": 100,  # Количество попыток
    "metric": "sharpe_ratio",
    "direction": "maximize",
    "n_jobs": 4,  # Параллельные процессы
    "random_state": 42,  # Для воспроизводимости
    "initial_capital": 10000.0,
    "commission": 0.001
}

response = requests.post(url, json=request_data)
task_id = response.json()["task_id"]
```

### Типы Параметров

#### 1. Integer (целочисленный)

```python
"period": {
    "type": "int",
    "low": 10,
    "high": 100,
    "step": 5  # Опционально
}
```

#### 2. Float (вещественный)

```python
"threshold": {
    "type": "float",
    "low": 0.001,
    "high": 0.1,
    "log": True  # Логарифмическая шкала (для параметров с большим диапазоном)
}
```

#### 3. Categorical (категориальный)

```python
"exit_type": {
    "type": "categorical",
    "choices": ["fixed", "trailing", "adaptive"]
}
```

### Важность Параметров

Bayesian optimization автоматически вычисляет importance каждого параметра:

```python
# Получить результаты
results = requests.get(f"http://localhost:8000/api/v1/optimize/{task_id}/results").json()

param_importance = results["results"]["param_importance"]
# {
#   "slow_period": 0.65,  # Самый важный
#   "fast_period": 0.25,
#   "threshold": 0.10     # Наименее важный
# }
```

**Интерпретация:**

- Высокая importance (>0.5): параметр сильно влияет на результат
- Низкая importance (<0.1): параметр можно зафиксировать

---

## ⚖️ Сравнение Методов

### Пример: Оптимизация 4 параметров

**Параметры:**

- `fast_period`: 5-50 (10 значений)
- `slow_period`: 20-200 (10 значений)
- `stop_loss`: 0.01-0.05 (5 значений)
- `take_profit`: 0.02-0.10 (5 значений)

**Total combinations:** 10 × 10 × 5 × 5 = **2,500 комбинаций**

| Метод                     | Iterations | Время (min) | Найденный Sharpe | Замечания                            |
| ------------------------- | ---------- | ----------- | ---------------- | ------------------------------------ |
| **Grid Search**           | 2,500      | 125 min     | 1.85             | Проверил все комбинации              |
| **Bayesian (n=100)**      | 100        | 5 min       | 1.82             | Нашёл 98% оптимума за 4% времени     |
| **Bayesian (n=250)**      | 250        | 12.5 min    | 1.86             | Превзошёл Grid Search за 10% времени |
| **Walk-Forward (5 окон)** | 5 × 2,500  | 625 min     | OOS Sharpe: 1.42 | Более реалистичная оценка            |

**Вывод:** Bayesian в 10-25 раз быстрее при сопоставимом качестве!

---

## 💡 Best Practices

### 1. Выбор Метода Оптимизации

```
┌─────────────────────────────────────────────────────────┐
│               DECISION TREE                             │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Параметров < 100 комбинаций?                          │
│         ├─ YES → Grid Search                           │
│         └─ NO  → Bayesian                               │
│                                                         │
│  Готовитесь к live trading?                            │
│         └─ YES → Walk-Forward (обязательно!)           │
│                                                         │
│  Проверяете новую стратегию?                           │
│         ├─ 1. Bayesian (найти хорошие параметры)       │
│         └─ 2. Walk-Forward (проверить устойчивость)    │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 2. Метрики Оптимизации

**Рекомендуемые метрики:**

| Метрика         | Когда Использовать            | Плюсы                        | Минусы                             |
| --------------- | ----------------------------- | ---------------------------- | ---------------------------------- |
| `sharpe_ratio`  | Сбалансированная оценка       | Учитывает риск               | Может быть негативным              |
| `sortino_ratio` | Фокус на downside risk        | Игнорирует upside volatility | Требует много данных               |
| `net_profit`    | Максимизация прибыли          | Простой                      | Не учитывает риск                  |
| `profit_factor` | Соотношение прибыли к убыткам | Интуитивный                  | Может быть undefined (нет убытков) |
| `max_drawdown`  | Минимизация риска             | Важно для risk management    | Минимизация → direction="minimize" |

⚠️ **Внимание:** Не используйте `win_rate` для оптимизации! Высокий win rate не означает прибыльность.

### 3. Размеры Выборок

**Grid Search:**

- Minimum: 1,000 свечей
- Recommended: 5,000+ свечей

**Walk-Forward:**

- Minimum: 10,000 свечей
- IS window: 500-1,000 свечей (2-4 месяца на 1h)
- OOS window: 250-500 свечей (1-2 месяца на 1h)
- Step: 125-250 свечей (0.5-1 месяц на 1h)

**Bayesian:**

- Minimum: 1,000 свечей
- n_trials: 50-200 (зависит от сложности)

### 4. Overfitting Prevention

⚠️ **Признаки Overfitting:**

- IS performance значительно лучше OOS (>20% разница)
- Очень высокий Sharpe ratio (>3.0)
- Слишком много сделок (>1000 на год)
- Идеальные результаты на истории

**Как Предотвратить:**

1. ✅ Используйте Walk-Forward для проверки
2. ✅ Ограничивайте пространство параметров разумными значениями
3. ✅ Добавьте комиссии и slippage
4. ✅ Тестируйте на разных периодах
5. ✅ Используйте простые стратегии

### 5. Computational Resources

**Grid Search:**

- CPU: 1 ядро на 1 комбинацию
- RAM: ~100MB на комбинацию
- Celery workers: равен количеству ядер

**Walk-Forward:**

- CPU: N окон × Grid Search
- RAM: 2x Grid Search
- Время: N окон × Grid Search время

**Bayesian:**

- CPU: n_jobs параллельных trials
- RAM: ~100MB на trial
- Оптимально: n_jobs = количество ядер - 1

---

## 📝 Примеры Использования

### Сценарий 1: Быстрый Поиск Параметров

**Цель:** Найти хорошие параметры за минимальное время

```python
# 1. Bayesian optimization (быстрый поиск)
response = requests.post("http://localhost:8000/api/v1/optimize/bayesian", json={
    "strategy_class": "MyStrategy",
    "symbol": "BTCUSDT",
    "timeframe": "1h",
    "start_date": "2024-01-01T00:00:00",
    "end_date": "2024-12-31T23:59:59",
    "parameters": {
        "period": {"type": "int", "low": 10, "high": 100},
        "threshold": {"type": "float", "low": 0.001, "high": 0.1, "log": True}
    },
    "n_trials": 100,
    "metric": "sharpe_ratio",
    "n_jobs": 4
})

task_id = response.json()["task_id"]

# 2. Дождаться результатов
import time
while True:
    status = requests.get(f"http://localhost:8000/api/v1/optimize/{task_id}/status").json()
    if status["status"] in ["SUCCESS", "FAILURE"]:
        break
    print(f"Progress: {status.get('progress', 0)}%")
    time.sleep(5)

# 3. Получить результаты
results = requests.get(f"http://localhost:8000/api/v1/optimize/{task_id}/results").json()
best_params = results["results"]["best_params"]
print(f"Best parameters: {best_params}")
print(f"Best Sharpe: {results['results']['best_value']:.4f}")
```

### Сценарий 2: Проверка Устойчивости

**Цель:** Убедиться что параметры работают стабильно

```python
# 1. Сначала найти хорошие параметры (Bayesian)
bayesian_response = ...  # См. Сценарий 1
best_params = bayesian_response["results"]["best_params"]

# 2. Проверить Walk-Forward
wf_response = requests.post("http://localhost:8000/api/v1/optimize/walk-forward", json={
    "strategy_class": "MyStrategy",
    "symbol": "BTCUSDT",
    "timeframe": "1h",
    "start_date": "2024-01-01T00:00:00",
    "end_date": "2024-12-31T23:59:59",
    "parameters": {
        # Узкие диапазоны вокруг найденных параметров
        "period": {
            "min": max(10, best_params["period"] - 10),
            "max": min(100, best_params["period"] + 10),
            "step": 5
        }
    },
    "in_sample_period": 120,
    "out_sample_period": 60,
    "metric": "sharpe_ratio"
})

# 3. Анализ результатов
wf_results = requests.get(f"http://localhost:8000/api/v1/optimize/{wf_task_id}/results").json()
summary = wf_results["results"]["summary"]

if summary["positive_window_rate"] > 0.6:
    print("✅ Стратегия устойчива!")
else:
    print("❌ Стратегия неустойчива, нужно пересмотреть")
```

### Сценарий 3: Production Pipeline

**Цель:** Полный цикл оптимизации перед live trading

```python
def optimize_for_production(strategy_class, symbol, timeframe):
    """
    Полный цикл оптимизации:
    1. Bayesian - быстрый поиск
    2. Walk-Forward - проверка устойчивости
    3. Валидация метрик
    """

    # Phase 1: Bayesian (широкий поиск)
    print("Phase 1: Bayesian optimization...")
    bayesian_task = start_bayesian_optimization(
        strategy_class=strategy_class,
        n_trials=200,
        param_space=FULL_PARAM_SPACE
    )
    best_params = wait_for_results(bayesian_task)

    # Phase 2: Walk-Forward (узкий поиск вокруг best_params)
    print("Phase 2: Walk-Forward validation...")
    wf_task = start_walkforward_optimization(
        strategy_class=strategy_class,
        param_space=narrow_space_around(best_params, margin=0.2),
        in_sample=120,
        out_sample=60
    )
    wf_results = wait_for_results(wf_task)

    # Phase 3: Validation
    summary = wf_results["summary"]

    checks = {
        "positive_windows": summary["positive_window_rate"] > 0.6,
        "oos_sharpe": summary["average_oos_sharpe_ratio"] > 1.0,
        "oos_profit": summary["total_oos_profit"] > 0,
        "stability": summary["std_oos_profit"] < summary["average_oos_profit"] * 0.5
    }

    if all(checks.values()):
        print("✅ Strategy ready for live trading!")
        return wf_results["windows"][-1]["best_params"]  # Последние лучшие параметры
    else:
        failed = [k for k, v in checks.items() if not v]
        print(f"❌ Strategy failed checks: {failed}")
        return None
```

---

## 📊 Интерпретация Результатов

### Grid Search Results

```json
{
  "best_params": {
    "fast_period": 15,
    "slow_period": 80
  },
  "best_metrics": {
    "sharpe_ratio": 1.85,
    "net_profit": 2500.5,
    "max_drawdown": -0.15,
    "total_trades": 45
  },
  "total_combinations": 100,
  "completed_combinations": 100
}
```

**Что Смотреть:**

- ✅ `sharpe_ratio > 1.0` - хорошо
- ✅ `max_drawdown < -0.25` - приемлемо
- ⚠️ `total_trades < 10` - мало данных, может быть overfitting
- ⚠️ `sharpe_ratio > 3.0` - слишком хорошо, вероятно overfitting

### Walk-Forward Results

```json
{
  "windows": [
    {
      "window_id": 0,
      "is_start": "2024-01-01",
      "is_end": "2024-04-30",
      "oos_start": "2024-05-01",
      "oos_end": "2024-06-30",
      "best_params": { "fast_period": 15, "slow_period": 80 },
      "oos_metrics": {
        "sharpe_ratio": 1.65,
        "net_profit": 450.25
      }
    }
    // ... more windows
  ],
  "summary": {
    "total_windows": 5,
    "positive_windows": 4,
    "positive_window_rate": 0.8, // 80% окон прибыльны
    "total_oos_profit": 2100.5,
    "average_oos_sharpe_ratio": 1.42,
    "std_oos_profit": 250.0
  }
}
```

**Что Смотреть:**

- ✅ `positive_window_rate > 0.6` - хорошая стабильность
- ✅ `std_oos_profit` низкий - стабильные результаты
- ⚠️ IS Sharpe значительно выше OOS Sharpe (>30% разница) - возможен overfitting
- ❌ `positive_window_rate < 0.5` - стратегия неустойчива

### Bayesian Results

```json
{
  "best_params": {
    "fast_period": 18,
    "slow_period": 95,
    "threshold": 0.0123
  },
  "best_value": 1.87,
  "best_trial": {
    "number": 67, // Нашёл на 67-й попытке из 100
    "user_attrs": {
      "net_profit": 2680.75,
      "total_trades": 52,
      "win_rate": 0.58,
      "max_drawdown": -0.18
    }
  },
  "param_importance": {
    "slow_period": 0.62, // Самый важный
    "fast_period": 0.28,
    "threshold": 0.1 // Наименее важный
  },
  "statistics": {
    "completed_trials": 98,
    "pruned_trials": 2, // Рано остановлены как неперспективные
    "failed_trials": 0
  }
}
```

**Что Смотреть:**

- ✅ Нашёл оптимум до завершения всех trials (эффективно)
- 📊 `param_importance` показывает на какие параметры фокусироваться
- ⚠️ Низкая importance (<0.1) - параметр можно зафиксировать
- ⚠️ Много pruned trials (>30%) - возможно слишком агрессивный pruner

---

## 🚀 Summary

**Quick Reference:**

```
Ситуация → Метод
────────────────────────────────────────────────────
Быстрый поиск               → Bayesian (100 trials)
Проверка устойчивости       → Walk-Forward (5 окон)
Маленькое пространство      → Grid Search
Подготовка к live trading   → Bayesian + Walk-Forward
Анализ важности параметров  → Bayesian
Полная картина параметров   → Grid Search
```

**Типичный Workflow:**

1. 🧠 Bayesian (200 trials) - найти region of interest
2. 🔲 Grid Search (узкий диапазон) - refined search
3. 🚶 Walk-Forward - проверка устойчивости
4. ✅ Validation - финальная проверка метрик
5. 🚀 Live Trading - с мониторингом

---

**Последнее Обновление:** October 17, 2025  
**Авторы:** Bybit Strategy Tester Team  
**Лицензия:** Internal Use Only
