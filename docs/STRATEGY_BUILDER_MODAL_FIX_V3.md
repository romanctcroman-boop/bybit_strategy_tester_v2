# 🔧 Strategy Builder - Исправление модального окна Templates V3

> **Дата**: 2026-01-29  
> **Проблема**: Модальное окно закрывается сразу после открытия из-за всплытия события клика

---

## 🔍 Корневая причина проблемы

### Из логов видно:

```
[Strategy Builder] Templates button clicked
[Strategy Builder] Opening modal
[Strategy Builder] Templates modal opened
[Strategy Builder] Modal overlay background clicked, closing  ← Проблема!
[Strategy Builder] Closing templates modal
```

**Причина**: Событие клика на кнопку Templates всплывает (bubbles) до overlay модального окна, который только что открылся, и вызывает его закрытие.

---

## ✅ Исправления применены

### 1. Использован capture phase для кнопки Templates

- Обработчик на кнопке использует capture phase (`true`)
- Используется `stopImmediatePropagation()` для полной остановки всплытия
- Добавлен флаг для отслеживания открытия через кнопку

### 2. Временная блокировка кликов на overlay

- При открытии модального окна добавляется временный обработчик
- Этот обработчик блокирует клики на overlay в течение 300ms
- После этого обработчик удаляется

### 3. Улучшен CSS для модального окна

- Добавлено `visibility: hidden/visible` для правильного отображения
- Улучшена анимация появления

### 4. Проверка флага открытия в overlay handler

- Overlay handler проверяет, не открывается ли модальное окно через кнопку
- Если да, клик игнорируется

---

## 🧪 Как проверить исправления

### Шаг 1: Перезагрузите страницу

```
Ctrl+F5 (для очистки кеша)
```

### Шаг 2: Откройте консоль браузера (F12)

### Шаг 3: Нажмите кнопку Templates

**Ожидаемые логи:**
```
[Strategy Builder] Templates button clicked
[Strategy Builder] Opening modal via button
[Strategy Builder] Opening templates modal
[Strategy Builder] Templates modal opened
[Strategy Builder] Modal classes: modal-overlay active
[Strategy Builder] Modal display: flex
[Strategy Builder] Templates rendered, HTML length: 5537, Templates in DOM: 7
[Strategy Builder] Temporary click blocker removed
[Strategy Builder] Modal confirmed open
```

**НЕ должно быть:**
```
[Strategy Builder] Modal overlay background clicked, closing  ← Этого не должно быть!
```

### Шаг 4: Проверьте модальное окно

Модальное окно должно:
- ✅ Открыться и остаться открытым
- ✅ Показать 7 шаблонов
- ✅ Не закрываться сразу после открытия
- ✅ Закрываться только при клике на overlay (темный фон) или кнопки Close/Cancel

---

## 🐛 Если модальное окно все еще закрывается

### Альтернативное решение - отключить закрытие по клику на overlay:

```javascript
// В консоли браузера - временно отключить закрытие по overlay
const modal = document.getElementById("templatesModal");
const oldHandler = modal.onclick;
modal.onclick = null;
console.log("Overlay click handler disabled");

// Открыть модальное окно
modal.classList.add("active");
```

### Или использовать только кнопки для закрытия:

Можно временно закомментировать обработчик overlay в коде, чтобы модальное окно закрывалось только через кнопки Close/Cancel.

---

## 📝 Измененные файлы

1. `frontend/js/pages/strategy_builder.js`
   - Использован capture phase для кнопки Templates
   - Добавлена временная блокировка кликов на overlay
   - Добавлен флаг для отслеживания открытия через кнопку

2. `frontend/css/strategy_builder.css`
   - Добавлено `visibility` для правильного отображения
   - Улучшена анимация

---

**Статус**: ✅ Исправления применены  
**Требуется**: Перезагрузка страницы и проверка работы модального окна
