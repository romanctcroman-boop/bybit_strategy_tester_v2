# ✅ ГОТОВО! Система настроена для открытия в системном браузере

## 🎯 Что было сделано:

### 1. **Настроен Vite для автооткрытия браузера**

Файл `frontend/vite.config.ts` обновлен:

```typescript
server: {
  port: 5173,
  open: true, // ← Автоматически открывает системный браузер
  // ...
}
```

### 2. **Созданы удобные скрипты запуска**

- ✅ `start_full_system.ps1` - Запуск Backend + Frontend + автооткрытие браузера
- ✅ `start_frontend.ps1` - Только Frontend
- ✅ `start_backend.ps1` - Только Backend

### 3. **Документация**

- ✅ `LAUNCH_SCRIPTS_README.md` - Подробные инструкции

---

## 🚀 Как запустить систему

### Вариант 1: Одной командой (Рекомендуется)

```powershell
.\start_full_system.ps1
```

**Что произойдет:**

1. Откроются 2 окна PowerShell (Backend, Frontend)
2. Backend запустится на http://localhost:8000
3. Frontend запустится на http://localhost:5173
4. **Системный браузер автоматически откроется** с приложением

---

### Вариант 2: Раздельный запуск

**Терминал 1 - Backend:**

```powershell
cd d:\bybit_strategy_tester_v2
python -m uvicorn backend.main:app --reload
```

**Терминал 2 - Frontend (откроется в браузере автоматически):**

```powershell
cd d:\bybit_strategy_tester_v2\frontend
npm run dev
```

Vite автоматически откроет http://localhost:5173 в вашем браузере по умолчанию!

---

### Вариант 3: Через новое окно PowerShell

```powershell
# Frontend
Start-Process powershell -ArgumentList "-NoExit -Command `"cd d:\bybit_strategy_tester_v2\frontend; npm run dev`"" -WindowStyle Normal

# Backend
Start-Process powershell -ArgumentList "-NoExit -Command `"cd d:\bybit_strategy_tester_v2; python -m uvicorn backend.main:app --reload`"" -WindowStyle Normal
```

---

## 🎉 Результат

**После запуска:**

1. ✅ Backend работает на http://localhost:8000
2. ✅ Frontend работает на http://localhost:5173
3. ✅ **Браузер открывается АВТОМАТИЧЕСКИ** (не Simple Browser VSCode!)
4. ✅ Hot Module Replacement активен

**Что вы увидите в браузере:**

- Фиолетовый AppBar с "⚡ Bybit Strategy Tester v2.0"
- Sidebar с 5 пунктами меню
- Dashboard с градиентными карточками статистики
- Полностью функциональное React приложение

---

## ⚙️ Настройка браузера по умолчанию

### Windows:

1. Откройте **Настройки** → **Приложения** → **Приложения по умолчанию**
2. Найдите ваш любимый браузер (Chrome, Firefox, Edge)
3. Установите его как браузер по умолчанию

### Или укажите браузер в vite.config.ts:

```typescript
server: {
  open: 'C:/Program Files/Google/Chrome/Application/chrome.exe', // Chrome
  // или
  open: 'C:/Program Files/Mozilla Firefox/firefox.exe', // Firefox
  // или просто
  open: true, // Браузер по умолчанию
}
```

---

## 🔥 Hot Module Replacement

При изменении кода:

1. Сохраните файл (Ctrl+S)
2. Браузер автоматически обновится БЕЗ перезагрузки страницы
3. State приложения сохраняется

**Тестируйте:**

- Измените цвет в Dashboard.tsx
- Добавьте текст в sidebar
- Создайте новый компонент
- Все изменения видны мгновенно!

---

## 🛑 Остановка

### Если запущено через start_full_system.ps1:

- Закройте 2 окна PowerShell (Backend и Frontend)

### Если запущено вручную:

- Нажмите `Ctrl+C` в терминалах

### Если нужно убить процессы:

```powershell
taskkill /F /IM node.exe    # Frontend
taskkill /F /IM python.exe  # Backend
```

---

## 📝 Полезные ссылки после запуска

- 🌐 **Frontend App:** http://localhost:5173
- 📚 **Backend API Docs:** http://localhost:8000/docs
- 📖 **ReDoc:** http://localhost:8000/redoc
- ❤️ **Health Check:** http://localhost:8000/health

---

## ✅ Проверка успеха

После запуска проверьте:

1. ✅ **Системный браузер открылся** (не Simple Browser в VSCode)
2. ✅ Видна страница с фиолетовой шапкой
3. ✅ Sidebar работает (можно переключаться между страницами)
4. ✅ Dashboard показывает карточки (даже если 0)
5. ✅ В консоли браузера (F12) нет критических ошибок

---

## 🎊 Готово!

Теперь приложение **всегда будет открываться в системном браузере**, а не во встроенном Simple Browser VSCode!

**Enjoy! 🚀**
