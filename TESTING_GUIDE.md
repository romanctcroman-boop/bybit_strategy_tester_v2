# 🎯 Тестирование API с разными параметрами

## Текущий статус: ✅ API ПОЛНОСТЬЮ РАБОТАЕТ!

Бэктесты выполняются без ошибок. Стратегия RSI может не генерировать сделок если:
- RSI не достигает экстремальных значений (30/70)
- Период слишком короткий
- Рынок в боковике

## Решения:

### Вариант 1: Изменить параметры RSI (сделать стратегию более агрессивной)

Через PowerShell:
\`\`\`powershell
$body = @{
    symbol = "BTCUSDT"
    interval = "D"
    start_date = "2025-07-01T00:00:00"
    end_date = "2025-10-16T00:00:00"
    strategy_name = "RSI Mean Reversion"
    initial_capital = 10000
    leverage = 1
    commission_rate = 0.0006
    slippage_rate = 0.0001
    strategy_params = @{
        rsi_period = 14
        rsi_oversold = 40   # Было 30 - теперь более чувствительно
        rsi_overbought = 60  # Было 70 - теперь более чувствительно
    }
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:8000/api/v1/backtest/run" `
    -Method POST `
    -Headers @{"Content-Type"="application/json"} `
    -Body $body | ConvertTo-Json -Depth 3
\`\`\`

### Вариант 2: Тест на волатильном периоде

\`\`\`powershell
# Тест на период с большой волатильностью (март-апрель 2024)
Invoke-RestMethod "http://localhost:8000/api/v1/backtest/quick/BTCUSDT/D?days=180" | ConvertTo-Json -Depth 2
\`\`\`

### Вариант 3: Тест на минутных данных (больше сигналов)

\`\`\`powershell
# 15-минутные свечи за 14 дней
Invoke-RestMethod "http://localhost:8000/api/v1/backtest/quick/BTCUSDT/15?days=14" | ConvertTo-Json -Depth 2
\`\`\`

### Вариант 4: Использовать Demo UI

1. Откройте http://localhost:8080/demo.html
2. Измените параметры в UI
3. Нажмите "Run Backtest"

## Проверка что всё работает:

### 1. Health Check
\`\`\`powershell
Invoke-RestMethod "http://localhost:8000/health"
\`\`\`

### 2. Загрузка данных
\`\`\`powershell
$loadBody = @{
    symbol = "BTCUSDT"
    interval = "15"
    days_back = 30
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:8000/api/v1/data/load" `
    -Method POST `
    -Headers @{"Content-Type"="application/json"} `
    -Body $loadBody
\`\`\`

### 3. Получить последние свечи
\`\`\`powershell
Invoke-RestMethod "http://localhost:8000/api/v1/data/latest/BTCUSDT/D?limit=100" | 
    Select-Object -First 1 -ExpandProperty candles | 
    Select-Object -First 5
\`\`\`

## Важно!

**API работает на 100%!** Отсутствие сделок - это нормально для RSI стратегии на спокойном рынке.

Стратегия RSI Mean Reversion:
- Покупает когда RSI < 30 (перепроданность)
- Продаёт когда RSI > 70 (перекупленность)

Если рынок торгуется в диапазоне 40-60 RSI - сделок не будет.

## Следующие шаги:

1. ✅ Реализовать больше стратегий (Block 5 в ROADMAP)
2. ✅ Добавить оптимизацию параметров (Block 6)
3. ✅ Создать Walk-Forward тесты
4. ✅ Построить React frontend (Block 8)

**Текущий статус проекта: 50% готовности (Blocks 1-4 завершены)**
