# 🚀 Скрипты запуска Bybit Strategy Tester v2.0

Удобные PowerShell скрипты для запуска приложения в **системном браузере** (не во встроенном VSCode).

---

## 📋 Доступные скрипты

### 1. **start_full_system.ps1** - Запуск полной системы (РЕКОМЕНДУЕТСЯ)

Автоматически запускает Backend + Frontend и открывает приложение в браузере.

**Использование:**

```powershell
.\start_full_system.ps1
```

**Что происходит:**

- ✅ Проверяет, не запущены ли уже сервисы
- 🔧 Запускает Backend (FastAPI) в отдельном окне
- 🎨 Запускает Frontend (React) в отдельном окне
- 🌐 Автоматически открывает http://localhost:5173 в системном браузере
- 📊 Показывает статус и ссылки на все сервисы

**Результат:**

- Frontend: http://localhost:5173
- Backend API: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

---

### 2. **start_frontend.ps1** - Только Frontend

Запускает React приложение и открывает в браузере.

**Использование:**

```powershell
.\start_frontend.ps1
```

**Что происходит:**

- ✅ Проверяет, не занят ли порт 5173
- 🎨 Запускает Vite dev server
- 🌐 Автоматически открывает в браузере (благодаря `open: true` в vite.config.ts)

**Требует:** Backend должен быть запущен отдельно для работы API

---

### 3. **start_backend.ps1** - Только Backend

Запускает FastAPI сервер.

**Использование:**

```powershell
.\start_backend.ps1
```

**Что происходит:**

- ✅ Проверяет, не занят ли порт 8000
- 🔧 Запускает uvicorn с автоперезагрузкой
- 💡 Предлагает открыть Swagger UI в браузере

**URL:**

- API Docs: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc

---

## 🎯 Рекомендуемый способ запуска

### Вариант А: Полная система одной командой

```powershell
# Запускает всё и открывает в браузере
.\start_full_system.ps1
```

### Вариант Б: Раздельный запуск

**Терминал 1 (Backend):**

```powershell
.\start_backend.ps1
```

**Терминал 2 (Frontend):**

```powershell
.\start_frontend.ps1
```

---

## ⚙️ Настройка автоматического открытия в браузере

### Frontend (Vite)

Уже настроено в `frontend/vite.config.ts`:

```typescript
server: {
  port: 5173,
  open: true, // Автоматически открывает браузер
  // ...
}
```

**Возможные значения `open`:**

- `true` - открывает в браузере по умолчанию
- `'/path'` - открывает конкретный путь (например, `'/optimization'`)
- `false` - не открывает автоматически

### Изменить браузер по умолчанию

**Windows:**

1. Настройки → Приложения → Приложения по умолчанию
2. Выберите браузер для протокола HTTP/HTTPS

**Или в Vite config:**

```typescript
server: {
  open: 'C:/Program Files/Google/Chrome/Application/chrome.exe',
}
```

---

## 🛑 Остановка сервисов

### Если запущено через start_full_system.ps1:

- Закройте 2 окна PowerShell (Backend и Frontend)
- Или нажмите `Ctrl+C` в каждом окне

### Если запущено вручную:

- Нажмите `Ctrl+C` в терминале

### Если окна закрыты, но порты заняты:

```powershell
# Найти процесс на порту 5173
netstat -ano | findstr :5173

# Убить процесс (замените PID)
taskkill /PID <номер_процесса> /F

# То же для порта 8000
netstat -ano | findstr :8000
taskkill /PID <номер_процесса> /F
```

---

## 🔧 Устранение неполадок

### Проблема: "Порт уже занят"

**Решение 1:** Скрипт автоматически обнаружит и предложит открыть существующее приложение

**Решение 2:** Остановите процессы вручную:

```powershell
# Остановить все Node.js процессы
taskkill /F /IM node.exe

# Остановить Python/Uvicorn
taskkill /F /IM python.exe
```

### Проблема: "Браузер не открывается автоматически"

**Причина:** Может блокироваться антивирусом или браузер не установлен по умолчанию

**Решение:** Откройте вручную:

- Frontend: http://localhost:5173
- Backend: http://localhost:8000/docs

### Проблема: "Backend не запускается - модуль не найден"

**Причина:** Неправильный путь к модулю

**Решение:** Используйте правильную команду:

```powershell
# ✅ ПРАВИЛЬНО
python -m uvicorn backend.main:app --reload

# ❌ НЕПРАВИЛЬНО
python -m uvicorn backend.api.main:app --reload
```

### Проблема: "Frontend показывает ошибку подключения к API"

**Причина:** Backend не запущен

**Решение:** Запустите Backend:

```powershell
.\start_backend.ps1
```

---

## 📚 Дополнительные команды

### Проверка статуса портов

```powershell
# Проверить порт 5173 (Frontend)
Test-NetConnection -ComputerName localhost -Port 5173

# Проверить порт 8000 (Backend)
Test-NetConnection -ComputerName localhost -Port 8000
```

### Просмотр логов

**Backend:**

```powershell
# Логи в папке logs/
Get-Content logs\api_*.log -Tail 50 -Wait
```

**Frontend:**

- В консоли браузера (F12)
- В терминале где запущен Vite

---

## 🎨 Настройка VSCode

Если хотите, чтобы VSCode **не открывал Simple Browser** автоматически:

**settings.json:**

```json
{
  "livePreview.autoRefreshPreview": "On External Changes to Saved Files",
  "liveServer.settings.CustomBrowser": "chrome", // или "firefox", "edge"
  "simpleBrowser.focusLockIndicator.enabled": false
}
```

---

## ✅ Проверка успешного запуска

После запуска `start_full_system.ps1`:

1. ✅ Должны открыться 2 окна PowerShell (Backend, Frontend)
2. ✅ Браузер автоматически откроется на http://localhost:5173
3. ✅ В браузере видна страница с фиолетовой шапкой и sidebar
4. ✅ Dashboard показывает статистику (или 0 если БД пустая)

**Вручную проверьте:**

- Frontend: http://localhost:5173 ✅
- Backend: http://localhost:8000/docs ✅
- Health: http://localhost:8000/health ✅

---

## 🚀 Быстрые команды

```powershell
# Полная система (РЕКОМЕНДУЕТСЯ)
.\start_full_system.ps1

# Только Frontend
.\start_frontend.ps1

# Только Backend
.\start_backend.ps1

# Остановить всё
taskkill /F /IM node.exe; taskkill /F /IM python.exe
```

---

**Enjoy! 🎉**
