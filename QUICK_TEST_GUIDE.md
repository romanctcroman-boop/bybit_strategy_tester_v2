# 🎯 Быстрая шпаргалка для тестирования

## Подготовка (1 минута)

1. **Приложение уже запущено:** http://localhost:5173
2. **Откройте консоль:** F12 → Console
3. **Загрузите тесты:**
   ```javascript
   // Скопируйте содержимое frontend/test_storage.js и вставьте в консоль
   ```

---

## Команды утилит

### Управление кэшем
```javascript
TestUtils.clearAllStorage()                    // 🗑️ Удалить ВСЕ данные
TestUtils.clearSymbol('BTCUSDT', '15')         // 🗑️ Удалить пару
TestUtils.showStats()                           // 📊 Показать статистику
TestUtils.createFakeCache('BTC', '15', 500)    // 🎭 Создать тестовые данные
TestUtils.partialClear('BTCUSDT', '15', 500)   // ✂️ Удалить последние N свечей
```

---

## Автоматические тесты

### 1. Пустая база → Загрузка 1000 свечей
```javascript
AutoTests.test1_EmptyStorage()
// Затем: перезагрузить страницу (Ctrl+R)
```

### 2. Пустая база + таймфрейм 1h
```javascript
AutoTests.test2_EmptyStorage1h()
// Затем: перезагрузить, переключить на 1h
```

### 3. Догрузка к существующему кэшу
```javascript
AutoTests.test3_PartialStorage()
// Затем: перезагрузить страницу
```

### 4. Догрузка + таймфрейм 1h
```javascript
AutoTests.test4_PartialStorage1h()
// Затем: перезагрузить, переключить на 1h
```

### 5. Исторические данные (проверка лимитов)
```javascript
AutoTests.test5_Historical()
// Ручная проверка через стор
```

### 6. Кнопка "Выход"
```javascript
AutoTests.test6_GracefulShutdown()
// Затем: нажать "Выход" в интерфейсе
```

### 7. Множественные таймфреймы
```javascript
AutoTests.test7_MultipleTimeframes()
```

### 8. Переключение пар
```javascript
AutoTests.test8_SymbolSwitch()
```

### 9. Лимит 2000 свечей
```javascript
AutoTests.test9_CacheLimits()
// Затем: перезагрузить страницу
```

---

## Эмодзи в логах (что искать в консоли)

| Эмодзи | Значение |
|--------|----------|
| 📦 | Найден кэш в localStorage |
| 🆕 | Кэш не найден, загрузка новых данных |
| 📊 | Загрузка с API Bybit |
| 🕐 | Временные метки (старая/новая свеча) |
| ✅ | Операция успешна |
| 💾 | Сохранение в localStorage |
| ⚠️ | Предупреждение |
| 🗑️ | Удаление данных |

---

## Типичные сценарии проверки

### Проверка после каждого теста
```javascript
TestUtils.showStats()  // Посмотреть что в кэше
```

### Сброс всего для чистого теста
```javascript
TestUtils.clearAllStorage()
// Затем: Ctrl+R (перезагрузка)
```

### Проверка конкретной пары
```javascript
// Посмотреть в localStorage
localStorage.getItem('bybit_candles_v1_linear_BTCUSDT_15')
```

### Создать тестовый сценарий
```javascript
// 1. Очистить всё
TestUtils.clearAllStorage()

// 2. Создать устаревший кэш
TestUtils.createFakeCache('BTCUSDT', '15', 800)
TestUtils.partialClear('BTCUSDT', '15', 200)  // Удалить последние 200

// 3. Перезагрузить и наблюдать
// Ctrl+R
```

---

## Что проверять в консоли

### ✅ ХОРОШО (ожидаемое поведение)
```
📦 Found 600 cached candles for: bybit_candles_v1_linear_BTCUSDT_15
🕐 Oldest: 2025-01-15T10:00:00Z, Newest: 2025-01-20T14:00:00Z
✅ Updated last 10 candles
📊 New candles needed: 150 (time diff: 135000 seconds)
📊 Fetching 150 new candles from API...
💾 Saved 750 candles to localStorage (deduplicated from 760)
```

### ❌ ПЛОХО (проблемы)
```
❌ ERROR: Failed to load candles
❌ ERROR: Failed to parse localStorage data
⚠️ Cache timestamp too old, ignoring...
```

---

## Быстрая диагностика проблем

### Проблема: Данные не загружаются
```javascript
// 1. Проверить кэш
TestUtils.showStats()

// 2. Очистить и попробовать снова
TestUtils.clearAllStorage()
```

### Проблема: Слишком много данных
```javascript
// Посмотреть размер
TestUtils.showStats()

// Если > 5MB - очистить старые
TestUtils.clearAllStorage()
```

### Проблема: Неправильные данные на графике
```javascript
// Очистить конкретную пару
TestUtils.clearSymbol('BTCUSDT', '15')

// Перезагрузить
// Ctrl+R
```

---

## DevTools (F12) - полезные вкладки

### Console
- Логи загрузки (📦📊✅💾)
- Выполнение тестовых команд

### Application → Storage → Local Storage
- Просмотр ключей `bybit_candles_v1_*`
- Ручное удаление/редактирование
- Проверка размера данных

### Network
- Запросы к API Bybit
- Проверка `bybitWorkingSet` вызовов
- Отключение сети для тестов (Offline mode)

---

## Контрольный чек-лист

### Перед началом тестов
- [ ] Приложение запущено на http://localhost:5173
- [ ] DevTools открыт (F12)
- [ ] Тестовый скрипт загружен в консоль
- [ ] `TestUtils` и `AutoTests` доступны

### После каждого теста
- [ ] Проверить консоль на ошибки
- [ ] Выполнить `TestUtils.showStats()`
- [ ] Проверить график визуально
- [ ] Записать результаты

### После всех тестов
- [ ] Очистить localStorage: `TestUtils.clearAllStorage()`
- [ ] Закрыть приложение
- [ ] Заполнить отчет в TESTING_PLAN.md

---

## Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| F12 | Открыть DevTools |
| Ctrl+R | Перезагрузить страницу |
| Ctrl+Shift+C | Инспектор элементов |
| Ctrl+L | Очистить консоль |
| Ctrl+Shift+Delete | Очистить данные браузера (осторожно!) |

---

## 🚀 Старт за 30 секунд

```javascript
// 1. Скопировать frontend/test_storage.js в консоль
// 2. Выполнить первый тест:
AutoTests.test1_EmptyStorage()
// 3. Ctrl+R (перезагрузка)
// 4. Наблюдать консоль!
```

**Полная документация:** `TESTING_PLAN.md`

**Удачи! 🎯**
