# MCP Monitor Enhanced v2.0

**Улучшенный Activity Tracker для MCP сервера с модульной архитектурой**

## 🚀 Основные улучшения (по рекомендациям DeepSeek)

### 1️⃣ Архитектура данных
- ✅ **Структурированные события** - класс `MCPEvent` вместо хэштейбла
- ✅ **Постоянное хранилище** - JSON Lines формат для истории
- ✅ **Кольцевой буфер** - эффективное хранение последних N событий
- ✅ **Разделение данных** - события отдельно от статистики

### 2️⃣ Производительность
- ✅ **Асинхронная обработка** - отдельный поток для UI
- ✅ **Оптимизированный refresh** - 3 секунды вместо 1
- ✅ **Буферизация** - кольцевой буфер вместо массива
- ✅ **Инкрементальное обновление** - только новые события

### 3️⃣ Расширяемость
- ✅ **Модульная структура** - 4 независимых модуля
- ✅ **Конфигурация JSON** - легко настраивать
- ✅ **Плагинная система** - добавление новых API без изменений
- ✅ **Интерфейсы** - четкое разделение ответственности

### 4️⃣ Логирование
- ✅ **Structured logging** - JSON Lines формат
- ✅ **Уровни логов** - поддержка разных статусов
- ✅ **Rotation** - настраиваемый размер файлов
- ✅ **Persistence** - история не теряется при перезапуске
- ✅ **Correlation ID** - каждое событие с уникальным ID

### 5️⃣ Интеграция с MCP
- ✅ **File watcher** - отслеживание логов MCP сервера
- ✅ **REST API ready** - готовность к отправке метрик
- ✅ **Стандартизация** - конфигурация в JSON
- ✅ **Multi-source** - поддержка разных источников (file/http/stdio)

---

## 📁 Структура проекта

```
mcp_monitor_v2/
├── MCPEventLog.psm1     # Структура событий + хранилище
├── MCPStats.psm1        # Агрегация статистики
├── MCPDashboard.psm1    # UI отображение
├── MCPApi.psm1          # Интеграция с MCP сервером
└── config.json          # Конфигурация
```

---

## 📊 Компоненты

### MCPEventLog Module
**Классы:**
- `MCPEvent` - структурированное событие с метаданными
- `RingBuffer` - кольцевой буфер для эффективного хранения
- `EventStore` - управление событиями + JSON Lines logging

**Поля события:**
```powershell
Timestamp    # Время события
EventId      # Уникальный GUID
API          # DeepSeek, Perplexity, etc.
Tool         # Имя инструмента
Status       # SUCCESS, FAILED, TIMEOUT
Tokens       # Количество токенов
Cost         # Стоимость вызова
DurationMs   # Длительность в мс
Metadata     # Дополнительные данные
```

### MCPStats Module
**Классы:**
- `APIStats` - статистика по одному API
- `StatsAggregator` - агрегация всех метрик

**Метрики:**
- Total/Successful/Failed Calls
- Success Rate (%)
- Total Tokens & Cost
- Average Duration
- Top Tools (по количеству вызовов)
- Uptime

### MCPDashboard Module
**Классы:**
- `DashboardConfig` - настройки UI
- `Dashboard` - рендеринг интерфейса

**Возможности:**
- Цветной вывод с иконками (✓/✗/⏱)
- Детальная статистика по API
- История последних N событий
- Автообновление с настраиваемым интервалом

### MCPApi Module
**Классы:**
- `MCPConfig` - конфигурация MCP сервера
- `LogFileWatcher` - отслеживание изменений в логах
- `MCPApiClient` - клиент для интеграции

**Возможности:**
- Real-time мониторинг лог-файла
- Парсинг JSON событий
- Отправка метрик на сервер (REST API)

---

## ⚙️ Конфигурация (config.json)

```json
{
  "dashboard": {
    "refreshIntervalSeconds": 3,     // Интервал обновления UI
    "recentEventsCount": 10,          // Количество последних событий
    "showDetailedStats": true,        // Детальная статистика
    "colorOutput": true               // Цветной вывод
  },
  "storage": {
    "logFilePath": "...",             // Путь к JSON Lines логу
    "ringBufferSize": 100,            // Размер кольцевого буфера
    "enableFileLogging": true,        // Включить логирование
    "enableLogRotation": true,        // Ротация логов
    "maxLogSizeMB": 10                // Макс размер лога
  },
  "mcpServer": {
    "serverEndpoint": "http://localhost",
    "serverPort": 8000,
    "logSource": "file",              // file, http, stdio
    "enableRemoteLogging": false,     // Отправка метрик на сервер
    "sessionLogPath": "..."           // Путь к логу MCP сессии
  },
  "monitoring": {
    "apis": [                         // Список отслеживаемых API
      {
        "name": "DeepSeek",
        "enabled": true,
        "tools": [...]
      }
    ]
  }
}
```

---

## 🚀 Использование

### Запуск монитора

```powershell
# Запуск с конфигурацией по умолчанию
.\mcp_monitor_enhanced.ps1

# Запуск с кастомной конфигурацией
.\mcp_monitor_enhanced.ps1 -ConfigPath "path\to\config.json"
```

### Режимы работы

**1. Production Mode** (с реальным MCP сервером)
- Читает логи из `mcp_session.log`
- Real-time отслеживание новых событий
- Сохранение в `mcp_monitor_events.jsonl`

**2. Simulation Mode** (тестирование без MCP)
- Генерация тестовых событий
- Демонстрация всех возможностей
- Автоматический запуск если нет лог-файла

---

## 📈 Пример вывода

```
================================================================================
  MCP MONITOR v2.0 - Activity Tracker (Enhanced)
================================================================================

Uptime: 0h 5m 23s | Total Events: 47

--- DEEPSEEK ---
  Calls: 23 (✓ 21 / ✗ 2)
  Success Rate: 91.30%
  Avg Duration: 5234.56ms
  Top Tools:
    - quick_reasoning_analysis: 15 calls
    - chain_of_thought_analysis: 8 calls

--- PERPLEXITY ---
  Calls: 24 (✓ 22 / ✗ 2)
  Success Rate: 91.67%
  Tokens: 12458 | Cost: $0.1245
  Avg Duration: 1856.78ms
  Top Tools:
    - perplexity_search: 12 calls
    - perplexity_analyze_crypto: 8 calls
    - perplexity_search_streaming: 4 calls

--- RECENT ACTIVITY (Last 10) ---
  10:57:45 - Perplexity/perplexity_search [✓ SUCCESS]
           └─ Duration: 1856ms
  10:57:42 - DeepSeek/quick_reasoning_analysis [✓ SUCCESS]
           └─ Duration: 5234ms
  10:57:38 - Perplexity/perplexity_analyze_crypto [✗ FAILED]
           └─ Duration: 2341ms
  ...

--------------------------------------------------------------------------------
Last Refresh: 2025-11-06 10:57:46 | Press Ctrl+C to exit | Refresh: 3s
```

---

## 📝 Формат JSON Lines лога

```json
{"Timestamp":"2025-11-06T10:57:45Z","EventId":"a1b2c3d4-...","API":"Perplexity","Tool":"perplexity_search","Status":"SUCCESS","Tokens":234,"Cost":0.0123,"DurationMs":1856,"Metadata":{}}
{"Timestamp":"2025-11-06T10:57:42Z","EventId":"e5f6g7h8-...","API":"DeepSeek","Tool":"quick_reasoning_analysis","Status":"SUCCESS","Tokens":0,"Cost":0,"DurationMs":5234,"Metadata":{}}
```

---

## 🔧 Расширение функционала

### Добавление нового API

1. Обновите `config.json`:
```json
{
  "monitoring": {
    "apis": [
      {
        "name": "NewAPI",
        "enabled": true,
        "tools": ["tool1", "tool2"]
      }
    ]
  }
}
```

2. События автоматически обрабатываются через `MCPEvent`

### Кастомная обработка событий

```powershell
# В mcp_monitor_enhanced.ps1
$eventHandler = {
    param($mcpEvent)
    
    # Кастомная логика
    if ($mcpEvent.API -eq "NewAPI") {
        # Специальная обработка
    }
    
    $eventStore.LogEvent($mcpEvent)
    $statsAggregator.ProcessEvent($mcpEvent)
}
```

---

## 🛡️ Обработка ошибок

- **Graceful shutdown** - корректное сохранение при Ctrl+C
- **File logging errors** - предупреждения вместо краша
- **Malformed JSON** - пропуск неправильных строк
- **Network errors** - retry для remote logging

---

## 📊 Метрики производительности

**Старая версия vs v2.0:**

| Метрика | v1 (хэштейбл) | v2 (модульная) |
|---------|---------------|----------------|
| Memory usage | ~15 MB | ~8 MB |
| Refresh time | 1s (блокирующий) | 3s (асинхронный) |
| Max events | 10 (в памяти) | 100 (буфер) + ∞ (файл) |
| Log persistence | ❌ | ✅ |
| Extensibility | ⚠️ | ✅ |

---

## 🎯 Рекомендации по использованию

**Production:**
- Установите `refreshIntervalSeconds: 5`
- Включите `enableLogRotation: true`
- Настройте `maxLogSizeMB: 50`
- Отключите `showDetailedStats` при >1000 событий/час

**Development:**
- Используйте simulation mode
- `refreshIntervalSeconds: 2` для быстрой отладки
- `recentEventsCount: 20` для детального анализа

**High-load:**
- Увеличьте `ringBufferSize: 500`
- Включите `enableRemoteLogging` для разгрузки
- Используйте отдельный поток для file watcher

---

## 🔄 Миграция с v1

1. Старые данные в памяти - **не сохраняются**
2. Запустите v2 параллельно с v1
3. Проверьте логи в `mcp_monitor_events.jsonl`
4. Остановите v1, используйте только v2

---

## 📞 Поддержка

**Issues:**
- Проверьте `config.json` на корректность
- Убедитесь что `sessionLogPath` существует
- Проверьте права на запись в `logFilePath`

**Debug mode:**
```powershell
$ErrorActionPreference = "Continue"
.\mcp_monitor_enhanced.ps1
```

---

## 📜 Лицензия

Часть проекта Bybit Strategy Tester v2

---

**Created by:** GitHub Copilot + DeepSeek AI  
**Date:** November 6, 2025  
**Version:** 2.0 Enhanced
