# 🤖 MCP Agent Orchestration System

## Автоматизация через Perplexity AI + Capiton GitHub

Эта система автоматизирует рабочие процессы проекта через интеграцию двух AI-агентов:
- **Perplexity**: глубокий анализ, синтез знаний, поиск решений
- **Capiton GitHub**: управление задачами, приоритизация, ограничения, интеграция с GitHub

---

## ⚡ Быстрый старт

### Windows (PowerShell)
```powershell
# 1. Установка MCP серверов
.\scripts\install_mcp.ps1

# 2. Настройка API ключей
# Отредактируйте .env файл:
# - PERPLEXITY_API_KEY=pplx-ваш-ключ
# - GITHUB_TOKEN=ghp_ваш-токен

# 3. Перезапустите VS Code
# MCP серверы запустятся автоматически!

# 4. (Опционально) Запуск вручную
Ctrl+Shift+P → Tasks: Run Task → MCP: Start All Servers
```

### Linux/Mac (Bash)
```bash
# 1. Установка MCP серверов
chmod +x scripts/install_mcp.sh
./scripts/install_mcp.sh

# 2. Настройка API ключей (см. выше)

# 3. Перезапустите VS Code
```

---

## 🏗️ Архитектура

### Разделение ответственности

#### Perplexity (Анализ)
✅ **ЧТО ДЕЛАЕТ:**
- Глубокий анализ аномалий и проблем
- Синтез знаний из документации
- Исследование решений
- Генерация кода и тестов

❌ **ЗАПРЕЩЕНО:**
- Создавать задачи
- Устанавливать приоритеты
- Выставлять ограничения

#### Capiton GitHub (Оркестрация)
✅ **ЧТО ДЕЛАЕТ:**
- Создание GitHub issues
- Приоритизация задач
- Управление ограничениями
- Координация code review
- Интеграция с GitHub (PR, labels, milestones)

❌ **ЗАПРЕЩЕНО:**
- Глубокий анализ (делегируется Perplexity)

### Workflow Pipeline

```
1. АНАЛИЗ (Perplexity)
   ↓
   Собран контекст, исследованы решения
   
2. ПЛАНИРОВАНИЕ (Capiton)
   ↓
   Созданы задачи, установлены приоритеты
   
3. ИСПОЛНЕНИЕ (Perplexity)
   ↓
   Решения реализованы
   
4. ВАЛИДАЦИЯ (Capiton)
   ↓
   Issues отслежены, ревью выполнены
```

---

## 📋 Примеры использования

### Автоматизированное исправление багов

**1. Запустите workflow:**
```powershell
# Через VS Code Task
Ctrl+Shift+P → Tasks: Run Task → Workflow: High Priority Anomalies

# Или через PowerShell
.\scripts\mcp_workflow.ps1
Start-AnomalyWorkflow -AnomalyNumbers @(4,5,6,7)
```

**2. Система автоматически:**
- Perplexity анализирует каждую аномалию
- Capiton создаёт 4 GitHub issues с приоритетами
- Perplexity исследует решения
- Capiton координирует выполнение
- Agent генерирует код + тесты
- Capiton создаёт PR и запрашивает ревью

### Текущий фокус: High Priority Anomalies (4-7)

**Аномалии:**
1. Position Sizing - динамический расчёт позиций
2. Signal Exit Logic - стратегии выхода
3. Buy & Hold - бенчмарк сравнение
4. Margin Calls - симуляция маржинальных рисков

**Автоматический процесс:**
```
Perplexity: Анализ каждой аномалии
    ↓
Capiton: Создание 4 GitHub issues (приоритет: HIGH)
    ↓
Perplexity: Исследование решений
    ↓
Capiton: Установка приоритетов и milestone
    ↓
Agent: Генерация кода
    ↓
Capiton: Создание PR и валидация
```

---

## 🛠️ Конфигурация

### MCP Серверы (`.vscode/mcp.json`)

```jsonc
{
  "mcpServers": {
    "perplexity": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-perplexity-ask"],
      "priority": "analysis",
      "autoStart": true
    },
    "capiton-github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-capiton-github"],
      "priority": "orchestration",
      "autoStart": true
    }
  },
  "workflow": {
    "orchestrator": "capiton-github",
    "analyzer": "perplexity",
    "autoExecution": true
  }
}
```

### Маршрутизация задач

| Тип задачи | Агент |
|------------|-------|
| Создание задач | Capiton |
| Приоритизация | Capiton |
| Ограничения | Capiton |
| Глубокий анализ | Perplexity |
| Поиск решений | Perplexity |
| Синтез знаний | Perplexity |
| Баг-расследование | Perplexity → Capiton |
| Code review | Capiton |
| Документация | Perplexity → Capiton |

---

## 📊 Мониторинг

### Проверка статуса MCP серверов

**PowerShell:**
```powershell
# Проверка установки
npm list -g | Select-String mcp

# Проверка процессов
Get-Process | Where-Object {$_.ProcessName -like "*node*"}

# Логи
Get-Content .vscode\mcp.log -Tail 50 -Wait
```

**Bash:**
```bash
# Проверка установки
npm list -g | grep mcp

# Проверка процессов
ps aux | grep node

# Логи
tail -f .vscode/mcp.log
```

### Метрики (в VS Code)
```
Ctrl+Shift+P → MCP: Show Metrics
```

Отслеживаемые метрики:
- Уровень выполнения задач (%)
- Глубина анализа (score)
- Время отклика (ms)
- Частота ошибок (%)

---

## 🚨 Устранение проблем

### MCP серверы не запускаются

**1. Проверьте установку:**
```powershell
npm list -g @modelcontextprotocol/server-perplexity-ask
npm list -g @modelcontextprotocol/server-capiton-github
```

**2. Переустановите при необходимости:**
```powershell
.\scripts\install_mcp.ps1
```

**3. Проверьте API ключи:**
```powershell
echo $env:PERPLEXITY_API_KEY
echo $env:GITHUB_TOKEN
```

### Агент не отвечает

**1. Перезапустите MCP серверы:**
```
Ctrl+Shift+P → Tasks: Run Task → MCP: Start All Servers
```

**2. Проверьте сетевое подключение**

**3. Проверьте валидность API ключей**

### Проблемы с GitHub интеграцией

**1. Проверьте области токена:**
- https://github.com/settings/tokens
- Необходимы: `repo`, `workflow`, `write:packages`, `read:org`

**2. Проверьте доступ к репозиторию:**
```bash
curl -H "Authorization: token $GITHUB_TOKEN" \
     https://api.github.com/repos/RomanCTC/bybit_strategy_tester_v2
```

---

## 📚 Документация

- **[MCP_SETUP_GUIDE.md](.vscode/MCP_SETUP_GUIDE.md)** - полное руководство по установке
- **[PROJECT_CONTEXT.md](.vscode/PROJECT_CONTEXT.md)** - контекст проекта для AI агентов
- **[mcp.json](.vscode/mcp.json)** - конфигурация MCP
- **[tasks.json](.vscode/tasks.json)** - задачи VS Code

### Полезные ссылки
- [MCP Documentation](https://modelcontextprotocol.io/)
- [Perplexity API Docs](https://docs.perplexity.ai/)
- [GitHub API Reference](https://docs.github.com/en/rest)
- [VS Code Tasks Guide](https://code.visualstudio.com/docs/editor/tasks)

---

## ✅ Текущий статус проекта

### Завершено (100%)
- ✅ Все 14 оригинальных задач
- ✅ Аномалии 1-3 (критические)
  - Code Consolidation: 100% тестов
  - RBAC Implementation: 89.5% тестов
  - DataManager Refactoring: 88.9% тестов
- ✅ Общие тесты: 25/29 (86.2%)

### В работе
- 🔄 Аномалии 4-7 (высокий приоритет)
  - Position Sizing
  - Signal Exit
  - Buy & Hold
  - Margin Calls
- 🔄 Walk-Forward тесты (4/7 падает)

### Оценка с MCP автоматизацией
**Без MCP**: 7 дней ручной работы  
**С MCP**: 2-3 дня (автоматизация анализа, кода, тестов, документации)

---

## 🎯 Best Practices

### DO ✅
- Позволяйте Capiton создавать все задачи
- Используйте Perplexity для глубокого анализа
- Проверяйте автоматические PR перед мержем
- Мониторьте метрики MCP регулярно

### DON'T ❌
- Не позволяйте Perplexity устанавливать ограничения
- Не переопределяйте приоритеты Capiton вручную
- Не отключайте auto-start в продакшене
- Не коммитьте .env с реальными ключами

---

## 🚀 Готов к работе!

**Статус**: ✅ READY TO USE  
**Последнее обновление**: 2025-01-27  
**Версия**: 1.0.0

**Следующий шаг**: Запустите аномалии 4-7!
```powershell
Ctrl+Shift+P → Tasks: Run Task → Workflow: High Priority Anomalies (4-7)
```
