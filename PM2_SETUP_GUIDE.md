# 🚀 PM2 Setup Guide - Automation System

## Что такое PM2?

PM2 - production-ready process manager для Node.js/Python приложений с:
- ✅ Автоматический restart при сбое
- ✅ Автозапуск при загрузке системы
- ✅ Мониторинг процессов в реальном времени
- ✅ Управление логами
- ✅ Clustering и load balancing

---

## 📦 Установка PM2

### Шаг 1: Установка Node.js (если нет)

```powershell
# Скачай и установи Node.js:
# https://nodejs.org/ (LTS версия)

# Проверь установку:
node --version
npm --version
```

### Шаг 2: Установка PM2

```powershell
# Установи PM2 глобально:
npm install pm2 -g

# Проверь установку:
pm2 --version
```

### Шаг 3: Установка PM2 Windows Startup (для автозапуска)

```powershell
# Установи pm2-windows-startup:
npm install pm2-windows-startup -g

# Настрой автозапуск:
pm2-startup install
```

---

## 🎯 Запуск Automation System через PM2

### Вариант 1: Использование ecosystem.config.js (РЕКОМЕНДУЕТСЯ)

```powershell
# Перейди в корень проекта:
cd D:\bybit_strategy_tester_v2

# Запусти все компоненты:
pm2 start ecosystem.config.js

# Сохрани конфигурацию для автозапуска:
pm2 save

# Проверь статус:
pm2 status
```

**Вывод должен быть:**
```
┌─────┬───────────────┬─────────┬─────────┬──────────┬────────┬──────┐
│ id  │ name          │ mode    │ ↺       │ status   │ cpu    │ mem  │
├─────┼───────────────┼─────────┼─────────┼──────────┼────────┼──────┤
│ 0   │ test-watcher  │ fork    │ 0       │ online   │ 0%     │ 45MB │
│ 1   │ audit-agent   │ fork    │ 0       │ online   │ 0%     │ 38MB │
└─────┴───────────────┴─────────┴─────────┴──────────┴────────┴──────┘
```

### Вариант 2: Ручной запуск каждого компонента

```powershell
# Test Watcher
pm2 start .venv\Scripts\python.exe --name test-watcher -- automation\task1_test_watcher\test_watcher.py

# Audit Agent
pm2 start .venv\Scripts\python.exe --name audit-agent -- automation\task3_audit_agent\audit_agent.py

# Сохрани конфигурацию:
pm2 save
```

---

## 🔍 Управление процессами

### Просмотр статуса

```powershell
# Статус всех процессов:
pm2 status

# Детальная информация о процессе:
pm2 show test-watcher

# Мониторинг в реальном времени:
pm2 monit
```

### Просмотр логов

```powershell
# Все логи в реальном времени:
pm2 logs

# Логи конкретного процесса:
pm2 logs test-watcher

# Последние 100 строк:
pm2 logs --lines 100

# Очистить логи:
pm2 flush
```

### Управление процессами

```powershell
# Restart всех процессов:
pm2 restart all

# Restart конкретного процесса:
pm2 restart test-watcher

# Stop всех процессов:
pm2 stop all

# Stop конкретного процесса:
pm2 stop test-watcher

# Reload (graceful restart):
pm2 reload all

# Delete процесса (удаление из PM2):
pm2 delete test-watcher

# Delete всех процессов:
pm2 delete all
```

---

## ⚙️ Автозапуск при загрузке Windows

### Настройка автозапуска

```powershell
# 1. Настрой PM2 startup:
pm2 startup

# 2. Запусти процессы:
pm2 start ecosystem.config.js

# 3. Сохрани текущую конфигурацию:
pm2 save

# 4. Перезагрузи систему и проверь:
pm2 status
```

### Проверка автозапуска

```powershell
# Проверь, что PM2 в автозагрузке:
pm2 startup

# Проверь сохранённую конфигурацию:
pm2 list
```

### Отключение автозапуска

```powershell
# Удали PM2 из автозагрузки:
pm2 unstartup

# Удали сохранённую конфигурацию:
pm2 delete all
pm2 save --force
```

---

## 📊 Мониторинг и метрики

### PM2 Plus (Web Dashboard) - ОПЦИОНАЛЬНО

```powershell
# Зарегистрируйся на pm2.io (бесплатно)

# Свяжи PM2 с dashboard:
pm2 link <secret_key> <public_key>

# Открой dashboard:
# https://app.pm2.io/
```

### Экспорт метрик в Prometheus

```powershell
# Установи плагин:
pm2 install pm2-prometheus-exporter

# Метрики доступны по адресу:
# http://localhost:9209/metrics
```

---

## 🛠️ Продвинутая конфигурация

### ecosystem.config.js - Опции

```javascript
{
  // Имя процесса
  name: 'my-app',
  
  // Скрипт для запуска
  script: 'python.exe',
  args: ['my_script.py'],
  
  // Автоперезапуск
  autorestart: true,
  max_restarts: 10,           // Макс. рестартов за 1 минуту
  min_uptime: '10s',          // Мин. время работы перед рестартом
  restart_delay: 5000,        // Задержка между рестартами (ms)
  
  // Память
  max_memory_restart: '512M', // Restart при превышении лимита
  
  // Логи
  error_file: './logs/error.log',
  out_file: './logs/out.log',
  log_date_format: 'YYYY-MM-DD HH:mm:ss',
  merge_logs: true,
  
  // Переменные окружения
  env: {
    NODE_ENV: 'production'
  },
  
  // Watch mode (автоперезапуск при изменении файлов)
  watch: false,
  ignore_watch: ['node_modules', 'logs'],
  
  // Cron restart (ежедневный restart в 3 AM)
  cron_restart: '0 3 * * *',
  
  // Clustering
  instances: 1,               // Количество инстансов (или 'max')
  exec_mode: 'fork'           // 'fork' или 'cluster'
}
```

---

## ✅ Тестирование автозапуска

### Test 1: Restart при сбое

```powershell
# 1. Запусти процесс:
pm2 start ecosystem.config.js

# 2. Убей процесс:
pm2 kill

# 3. Проверь, что PM2 автоматически перезапустил:
pm2 status
# Должно быть: status = online, restart > 0
```

### Test 2: Restart при превышении памяти

```python
# Добавь в test_watcher.py для теста:
import time
memory_leak = []
while True:
    memory_leak.append('x' * 1024 * 1024)  # 1 MB per iteration
    time.sleep(1)
```

```powershell
# PM2 автоматически убьёт и перезапустит процесс при превышении max_memory_restart
pm2 logs test-watcher
```

### Test 3: Автозапуск при загрузке

```powershell
# 1. Сохрани конфигурацию:
pm2 save

# 2. Перезагрузи систему:
Restart-Computer

# 3. После загрузки проверь:
pm2 status
# Процессы должны быть online
```

---

## 🔧 Troubleshooting

### Проблема: PM2 не запускается после перезагрузки

```powershell
# Проверь startup скрипт:
pm2 startup

# Переустанови startup:
pm2 unstartup
pm2 startup
pm2 save
```

### Проблема: Процессы постоянно рестартятся

```powershell
# Проверь логи:
pm2 logs --lines 50

# Увеличь min_uptime в ecosystem.config.js:
min_uptime: '30s'

# Проверь лимит памяти:
max_memory_restart: '1G'
```

### Проблема: PM2 не находит Python

```powershell
# Укажи полный путь к Python в ecosystem.config.js:
script: 'D:\\bybit_strategy_tester_v2\\.venv\\Scripts\\python.exe'
```

---

## 📋 Checklist - PM2 Setup Complete

- [ ] Node.js установлен
- [ ] PM2 установлен глобально (`npm install pm2 -g`)
- [ ] pm2-windows-startup установлен
- [ ] `ecosystem.config.js` создан
- [ ] Процессы запущены (`pm2 start ecosystem.config.js`)
- [ ] Конфигурация сохранена (`pm2 save`)
- [ ] Автозапуск настроен (`pm2 startup`)
- [ ] Автозапуск протестирован (перезагрузка системы)
- [ ] Логи проверены (`pm2 logs`)
- [ ] Мониторинг работает (`pm2 monit`)
- [ ] Документация прочитана ✅

---

## 🎊 Готово!

После выполнения всех шагов:
- ✅ Компоненты запускаются автоматически при загрузке Windows
- ✅ Автоматический restart при сбое
- ✅ Автоматический restart при превышении памяти
- ✅ Логирование и мониторинг
- ✅ Управление процессами через CLI

**Система готова к production! 🚀**
