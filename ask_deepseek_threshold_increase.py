"""
Консультация с DeepSeek Agent: как увеличить Virtual Tools Threshold до 140-150
"""

import asyncio
import sys
from pathlib import Path
from dotenv import load_dotenv

# Load environment variables
load_dotenv()

# Add project root to path
sys.path.insert(0, str(Path(__file__).parent))

from backend.agents.unified_agent_interface import (
    get_agent_interface,
    AgentRequest,
    AgentType,
)
from loguru import logger


async def ask_deepseek_threshold_increase():
    """Спросить DeepSeek как увеличить Virtual Tools Threshold"""
    
    interface = get_agent_interface()
    
    prompt = """
# КОНСУЛЬТАЦИЯ: КАК УВЕЛИЧИТЬ VIRTUAL TOOLS THRESHOLD ДО 140-150

## Контекст проблемы

**Текущая ситуация:**
- GitHub Copilot показывает: "133 tools активно vs 128 threshold"
- Предупреждение: "degraded tool calling once the threshold is hit"
- Распределение: ~40-50 MCP tools + ~80 стандартных Copilot tools

**Предыдущая рекомендация:**
DeepSeek Agent рекомендовал гибридный подход (C + A):
1. Деактивировать 5-7 редко используемых tools
2. Если проблемы остаются - увеличить threshold до 140-150

**Требование пользователя:**
> "Удалять не чего не хочется. Но и расширить вряд ли получится 'увеличить threshold до 140-150', надо проверять."

## Вопросы требующие детального ответа

### 1. ТЕХНИЧЕСКИЕ ДЕТАЛИ УВЕЛИЧЕНИЯ THRESHOLD

**1.1 Конфигурация VS Code:**
- Какой конкретно параметр в `.vscode/settings.json` нужно изменить?
- Существует ли параметр `github.copilot.chat.virtualTools.threshold`?
- Если нет - какие альтернативные способы увеличения threshold?
- Требуется ли перезапуск VS Code после изменения?

**1.2 Ограничения:**
- Есть ли жёсткий максимум для threshold (например, 200 или 256)?
- Может ли GitHub Copilot вообще работать с 150+ tools?
- Зависит ли ограничение от версии VS Code / Copilot Extension?
- Есть ли документация Microsoft/GitHub по этому параметру?

**1.3 Альтернативные методы:**
- Можно ли отключить предупреждение "degraded tool calling"?
- Существует ли параметр `github.copilot.chat.virtualTools.enabled: false`?
- Можно ли увеличить через environment variables или command line flags?

### 2. РИСКИ И ПОСЛЕДСТВИЯ УВЕЛИЧЕНИЯ THRESHOLD

**2.1 Performance Impact:**
- Как увеличение до 150 повлияет на latency ответов Copilot?
- Увеличится ли потребление RAM / CPU?
- Какая разница в performance между 128 vs 150 tools? (количественно)
- Есть ли risk of context window overflow при 150 tools?

**2.2 Tool Calling Quality:**
- Ухудшится ли accuracy выбора правильного tool при 150 доступных?
- Может ли модель "теряться" среди большого количества tools?
- Какие метрики для мониторинга деградации качества? (конкретные примеры)

**2.3 Stability Risks:**
- Может ли увеличение threshold вызвать crashes VS Code?
- Есть ли известные bugs при threshold > 130?
- Влияет ли это на другие функции Copilot (code completion, inline suggestions)?

### 3. РЕКОМЕНДУЕМАЯ СТРАТЕГИЯ

**3.1 Пошаговый план:**
Если увеличение threshold технически возможно:
1. Какой оптимальный первый шаг? (140? 150? Постепенно по +10?)
2. Как правильно тестировать изменения?
3. Какие baseline metrics собрать перед изменением?
4. Как rollback если что-то пойдёт не так?

**3.2 Best Practices:**
- Есть ли примеры проектов с 150+ tools работающих стабильно?
- Какие настройки рекомендуют для large tool sets?
- Нужна ли дополнительная оптимизация MCP servers?

**3.3 Monitoring Plan:**
- Какие конкретные метрики отслеживать после изменения?
- Как определить что threshold слишком высокий?
- Признаки того что нужно откатиться назад?

### 4. ПРАКТИЧЕСКАЯ РЕАЛИЗАЦИЯ

**4.1 Готовый код конфигурации:**
Пожалуйста предоставь готовую конфигурацию `.vscode/settings.json` для:
- Увеличения threshold до 140
- Увеличения threshold до 150
- Отключения virtual tools (если это альтернатива)
- Настройки для optimal performance при 150 tools

**4.2 Validation скрипт:**
Можешь ли предложить Python скрипт для:
- Проверки текущего количества активных tools
- Мониторинга latency до/после изменения
- Автоматического rollback при деградации

**4.3 Troubleshooting:**
- Что делать если threshold не увеличивается (параметр игнорируется)?
- Как проверить что новый threshold применён?
- Fallback strategies если увеличение невозможно?

## Ожидаемый формат ответа

1. **Прямой ответ**: Можно ли увеличить threshold до 140-150? (Да/Нет + обоснование)
2. **Технические инструкции**: Точный код конфигурации если возможно
3. **Риски**: Количественная оценка performance impact
4. **Альтернативы**: Если увеличение невозможно - что делать?
5. **Мониторинг**: Конкретные метрики и пороговые значения

## Требования к ответу

- ✅ Конкретные параметры конфигурации (не абстрактные советы)
- ✅ Количественные оценки (не "может замедлиться" а "замедление на X%")
- ✅ Готовый код для копирования
- ✅ Практические примеры из реальных проектов
- ✅ Пошаговый план реализации с временными оценками

**Критически важно**: Нужен actionable ответ для немедленной реализации, не теоретический анализ.
"""

    logger.info("🚀 Запрос консультации у DeepSeek Agent...")
    logger.info("❓ Вопрос: Как технически увеличить Virtual Tools Threshold до 140-150?")
    logger.info("=" * 80)
    
    request = AgentRequest(
        agent_type=AgentType.DEEPSEEK,
        task_type="technical_consultation_threshold",
        prompt=prompt,
        context={
            "current_tools": 133,
            "current_threshold": 128,
            "desired_threshold": 150,
            "user_requirement": "не хочу удалять tools, хочу увеличить threshold",
            "previous_recommendation": "hybrid approach C+A"
        }
    )
    
    logger.info("📨 Отправка запроса в DeepSeek Agent...")
    logger.info(f"📝 Длина prompt: {len(prompt)} символов")
    
    response = await interface.send_request(request)
    
    if response.success:
        logger.success(f"✅ DeepSeek ответил за {response.latency_ms}ms")
        logger.info(f"📊 Channel: {response.channel}, API key: #{response.api_key_index}")
        logger.info("=" * 80)
        logger.info("📄 ОТВЕТ DEEPSEEK:")
        logger.info("=" * 80)
        print(response.content)
        logger.info("=" * 80)
        
        # Сохранить в файл
        output_file = Path(__file__).parent / "DEEPSEEK_THRESHOLD_INCREASE_GUIDE.md"
        with open(output_file, "w", encoding="utf-8") as f:
            f.write("# Как увеличить Virtual Tools Threshold до 140-150\n\n")
            f.write(f"**Дата консультации**: 2025-11-11\n\n")
            f.write(f"**Вопрос**: Технические детали увеличения GitHub Copilot Virtual Tools Threshold с 128 до 140-150\n\n")
            f.write(f"**Latency**: {response.latency_ms}ms\n\n")
            f.write("---\n\n")
            f.write(response.content)
        
        logger.success(f"💾 Ответ сохранён в: {output_file}")
        return response.content
    else:
        logger.error(f"❌ DeepSeek не смог ответить: {response.error}")
        return None


if __name__ == "__main__":
    logger.info("🚀 Запуск детальной консультации о увеличении Virtual Tools Threshold...")
    logger.info("🎯 Цель: Получить конкретные инструкции для увеличения threshold до 140-150")
    logger.info("=" * 80)
    
    result = asyncio.run(ask_deepseek_threshold_increase())
    
    if result:
        logger.success("✅ Консультация завершена успешно!")
        logger.info("📄 Результаты сохранены в: DEEPSEEK_THRESHOLD_INCREASE_GUIDE.md")
    else:
        logger.error("❌ Не удалось получить ответ от DeepSeek")
